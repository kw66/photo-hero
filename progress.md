Original prompt: 继续和我讨论游戏事宜，我们到时候上传github和研究生模拟器一样让大家玩吗。要不先做个雏形，让我先能在我的手机上打开网页调用相机，调用大模型？

## 2026-05-14

- Decision: prototype first, no hosted backend. Use browser H5 for pure photo capture and a local Node service for OpenAI-compatible vision calls.
- Project display name: 照片勇者.
- Scope: API config, pure photo capture, one equipment identification flow, simple equip/fight loop.
- Created package.json, server.js, and the first public UI files.
- User correction: no realtime camera preview. The game should be pure photo-taking.
- Verification note: inline PowerShell JSON for Playwright actions failed due quoting, so use tests/idle-actions.json.
- Installed dependencies.
- Started local server on port 3000.
- Verified desktop page load, app.js load, attack interaction, mobile photo-input flow, mock equipment generation, and missing-parameter API error path.
- Added README.md and .gitignore.
- Removed Playwright from project dependencies; verification uses the local Codex web-game skill instead.
- Direction correction: final public version must be a single GitHub Pages style URL, not a local Node app.
- API config must be stored only in each user's browser localStorage.
- Changed root structure to static-first index.html/app.js/styles.css.
- Changed photo input to allow camera or gallery instead of forcing camera capture.

## TODO

- Test phone photo capture on a real device over LAN.
- Test with a real OpenAI-compatible vision API key.
- Verify GitHub Pages style static serving.
- Current preview server PID after static-first rewrite: 14392.
- Added text-only LLM chat test before photo/vision testing.
- GitHub CLI is installed but not authenticated on this machine; deployment is blocked until `gh auth login` is completed.
- Added API provider preset buttons for Ali Qwen-VL, SiliconFlow, DeepSeek text test, and Custom mode.
- URL/model fields now start empty, lock when a preset is selected, and unlock when Custom is selected.
- 2026-05-23: Updated monster synergy traits: guard team shields, warrior warcry, wizard defense break, patrol shield break, and golem sturdy defense floor. Verified with targeted Playwright checks and `npm run qa:review`.
- Custom mode preserves the user's manually typed URL/model while switching between presets.
- Added player-facing guidance that photo identification requires vision/multimodal support, OpenAI-compatible chat/completions, and browser CORS.
- Verified with `node --check app.js`, local page load through the web-game Playwright client, and a mobile Playwright flow covering preset locking, custom restore, refresh persistence, and API key eye-toggle behavior.
- Added official provider links for preset APIs: Ali Bailian console/key docs, SiliconFlow console/docs, and DeepSeek platform/docs; verified link switching and mobile overflow.
- Reworked the game UI into a one-panel, desktop-first control board: API settings are hidden behind a top button, the hero area owns attributes/equipment/photo identification, and the enemy area owns combat actions and battle report.
- Replaced the three-slot RPG equipment model with a Mota-style stat model: HP bar, EXP bar, level, speed, attack, defense, and shield.
- Equipment is now an unlimited collection: all identified items contribute stats, the UI shows 10 thumbnail slots per page, supports paging, and clicking a slot shows a larger image plus description and stat pills.
- Verified desktop 1440x900 and laptop 1366x768 fit in one page without document scrolling; mobile has no horizontal overflow and keeps natural vertical browsing.
- Desktop UI refinement: compressed the title/header into a 40px tool strip, renamed the player display to 照片勇者, reduced button and type scale, and changed player stats from large cards into a compact status strip.
- Enemy UI refinement: replaced the wide enemy banner with a compact square-ish enemy card prepared for multiple enemies, added enemy speed, and simplified battle actions to 开战 / 逃跑.
- Verified `node --check app.js`, desktop 1366x768 and 1440x900 no document scroll, and interaction flow for 开战 / 逃跑 with no console errors.
- Removed the DeepSeek text-only preset from the UI and hardened config loading so old saved DeepSeek preset values fall back to custom mode instead of lingering in the interface.
- Simplified player progression around photo equipment only: removed player level/EXP/progress growth, kept seven player stats (HP, attack, defense, speed, regen, shield, lifesteal), and made regen/lifesteal reduce battle attrition without adding a larger economy.
- Implemented the agreed battle semantics: speed is an action-timeline interval (`1 / speed`) with same-time hero priority, shield refreshes per enemy and absorbs damage before HP, regen triggers after being hit, and lifesteal triggers after hero attacks.
- Added a temporary attribute test panel for the seven player stats so battle tuning can be tested without using equipment generation.
- Reworked 图文战报 into structured battle reports: stat/API/debug logs no longer appear, equipment acquisition still appears, active battle details stay visible, and finished battles collapse into clickable summaries with life change and reward text.
- Changed 连续战斗 so it starts the first round immediately, has no visible stop mode, disables manual battle while running, and only stops on hero defeat or player escape; enemy victory and round-50 enemy escape roll into the next monster.
- Added the round-50 anti-stall rule: when the action counter reaches 50, the current enemy flees, a summary is recorded, and the next enemy appears.
- Verified direct local HTML (`file:///C:/Users/KW/Desktop/photo-hero/index.html`) for one-round battle text, escape summary collapse/expand, immediate continuous battle, escape stopping continuous battle, stat-change log filtering, mock-equipment acquisition log, victory summary collapse/expand, and round-50 enemy flee. Also verified `node --check app.js` and screenshot output under `output/battle-report-qa`.
- Simplified combat controls again: removed the separate 连续战斗 button, kept one 战斗 button that runs the current encounter continuously, and stops after victory, defeat, escape, or the round-50 enemy flee. The next monster appears but is not auto-fought until the player clicks 战斗 again.
- Removed the empty battle-report placeholder sentence so 图文战报 stays blank before there is an actual battle summary, active battle, or item acquisition.
- Verified direct local HTML for the two-button layout, blank initial battle report, immediate battle start, victory stop without continuing to the next monster, round-50 enemy flee stop, and screenshot output under `output/single-battle-qa`.
- Fixed speed scheduling: each side's first action is now scheduled at `1 / speed` instead of both starting at time 0. This makes high speed act earlier/more often while preserving hero priority on exact ties.
- Verified speed cases through local HTML automation: hero speed 5 vs monster 12 starts with monster, monster, hero; hero 12 vs monster 5 starts with hero, hero, monster; equal speed starts with hero. Also migrated old `0/0` round-1 clocks to the new schedule.
- Replaced the single-monster loop with the new 40-floor tower rules: deterministic item+monster names, fixed monster stats/traits, fixed item drops, 3-monster normal floors, boss/reward-boss floors, left-to-right multi-enemy selection, simultaneous enemy turns, hero target order, and per-kill round-50 counter reset.
- Changed equipment to selected-only stat application for fixed drops, preserved the user-specified item values without old rarity caps, and added `胶卷` gating for real photo identification. The mock identification button remains a development shortcut.
- Added save version `2` so old single-monster saves are discarded instead of corrupting the new tower state. New default hero stats are `50/10/3/5` for HP/attack/defense/speed to keep the first tower loop playable with the requested monster table.
- Verified direct local HTML with `node --check app.js`, the develop-web-game Playwright client, and targeted Playwright scripts for fresh load, selecting 1-3 monsters, battle tick ordering, in-battle escape, boss no-escape/full-selection, floor 10 and 25 composition, reward-boss skip, item drop/equip, selected-only equipment stats, round-50 reset after kill, and fixed item values (`雨伞 +8`, `西红柿 +15`).
- Updated monster selection from left-prefix selection to arbitrary click/toggle selection. Enemy cards now show selection order numbers, combat uses that order for hero targeting, all selected enemies still attack the hero, and victory/escape advances directly to the next floor so the remaining unselected monsters cannot be fought afterward.
- Removed `胶卷` from the normal item-prefix/drop table. Normal monsters now drop their fixed item plus 1 `胶卷碎片`; 10 shards synthesize 1 film use. Boss/reward-boss monsters can additionally drop a special film equipment item (`骷髅队长胶卷`, `吸血鬼胶卷`, `骑士队长胶卷`, `章鱼胶卷`, `魔龙胶卷`, `大法师胶卷`) with the user-specified stats.
- Changed `骑士队长` trait from red-lotus/no-regen to `护盾40`, and bumped save version to `3` to discard older tower saves with incompatible selection/reward state.
- Verified arbitrary selection/cancel/reorder, selected-order hero targeting, automatic floor advance after selected enemies are cleared, normal item + shard rewards, shard synthesis, boss auto-selection, `骑士队长` shield 40, boss film stats/equipment effect, and film consumption priority through direct local HTML Playwright scripts. Latest screenshots are under `output/selection-rule-qa`.
- Updated fixed item values: `雨伞` shield changed to 6 and `镜子` shield changed to 3.
- Reworked equipment from selected-only to backpack plus equipped slots: backpack is unlimited, up to 10 items can be equipped, clicking an item toggles equip/unequip, equipped item stats stack, and the UI shows equipped count plus an `装` badge.
- Added item value rules for photo identification. Model prompt now asks for `value` 5-20, `tooLarge`, and stats under weighted costs: hp 1, attack 5, defense 6, speed 18, shield 4, lifesteal 12, regen 12. The prompt also asks for realism/clarity/subject-size/interestingness quality and rejects person-scale-or-larger objects and scenery as value 0.
- Added local validation for model outputs: too-large items are forced to value 0 and all-zero stats; non-fixed photo items are clamped to value 5-20 and stats are trimmed to the weighted value budget; empty stats get a property-based fallback allocation from item name keywords.
- Verified with Playwright that fixed shield values are 6/3, oversized photo items produce zero stats and do not auto-equip, over-budget model stats are clamped, fallback allocation works, 10 equipped item cap is enforced, clicking again unequips, stacked stats update, and real `analyzePhoto` consumes film while preserving zero-value backpack entries.
- Changed the default hero stats to HP 50, attack 3, defense 1, speed 2, and bumped save version to `4` so older saves with previous base stats are discarded.
- Removed the old post-defeat revival path. When the hero reaches 0 HP, battle, flee, and floor advance are blocked at both UI and function level; the only normal recovery path is reset/restart. Verified with direct local HTML Playwright checks and screenshot output under `output/defeat-lock-qa`.
- Updated the SiliconFlow preset from `Qwen/Qwen2.5-VL-72B-Instruct` to `Qwen/Qwen3-VL-32B-Instruct`, added the SiliconFlow model center link, and verified with `node --check app.js` plus the direct local HTML web-game smoke test.
- After the user hit `Model disabled.`, changed the SiliconFlow preset again to `Qwen/Qwen3.6-35B-A3B`, which the current model center shows as a vision-capable option, and normalized `Model disabled` errors into a Chinese hint that asks the player to choose an enabled vision model from their own account console.
- Added `initialFilmRolls = 1`, bumped save version to `5`, and verified fresh local HTML starts with `filmRolls: 1`, `filmCount: 1`, and the UI text `胶卷 1 · 碎片 0/10`.
- Integrated the new `figure/` pixel assets: cropped `011-Braver01.png` into `figure/hero-front.png`, mapped all 19 monster types to existing image files including the newly added knight `037-03.png`, and verified no mapped monster image is missing.
- Reworked enemy cards to show monster portraits, selection badges in the top-right corner, and separate prefix drop effects versus monster traits. Verified with local HTML screenshots under `output/figure-ui-qa` and state output containing `image` and `prefixEffect` per enemy.
- Added a temporary film-shard control to the attribute test panel and verified `+ + -` results in `filmShards: 1` with the UI badge `胶卷 1 · 碎片 1/10`.
- Merged the separate photo/upload panel into the equipment library header and reused the equipment detail image frame for pending photo preview, removing the unused standalone camera space and the placeholder text `选择装备查看大图`.
- Applied the latest desktop UI/logic cleanup: enemy prefixes now modify stats, boss floors force all enemies selected and disable fleeing, the top title was removed, API settings/restart moved into the hero row, HP text shares the health bar row, enemy section title and per-enemy drop text were removed, and the HP bar picked up a more polished gradient/animation style.
- Fixed the pending photo preview path in the equipment detail frame by explicitly clearing the hidden state and prioritizing the selected photo over any current equipment until鉴定 completes.
- Updated reward and backpack presentation: battle summaries now merge repeated rewards such as `胶卷碎片*3`, the photo/film badge no longer says `已选择`, backpack display auto-sorts by item value, the visible equipment page is now a larger 3x3 grid while the equip cap stays 10, and the equipment detail image frame is larger.
- Fixed monster drop item IDs so multiple drops from one battle stay distinct, can all be equipped, and sort correctly after being added to the backpack.
- Cleaned up the player stat strip for desktop: removed the duplicate HP stat because the HP bar already shows life, ordered the visible stats as attack/defense/speed/regen/lifesteal/shield, and aligned the temporary test controls with the same lifesteal-before-shield order.
- Reworked enemy cards so the monster type is not repeated as a separate line. The two rows under each enemy name now show the concrete equipment prefix plus its real stat effect, then the monster traits.
- Hardened prefix stat normalization for old saves and fresh encounters. Fixed equipment prefixes such as `橡皮` now change enemy stats (`橡皮蝙蝠` gets defense +2) and the effect text reflects the applied value.
- Replaced fixed/system equipment placeholder images with Twemoji CDN icons for item drops and boss film items; local photo equipment still uses the actual uploaded image.
- Removed the Ali Qwen preset and made SiliconFlow the main preset with a model dropdown containing the currently discussed Kimi/Qwen vision model names. Custom mode still allows manual URL/model entry.
- Added advanced API controls for max tokens, image detail, thinking mode, thinking budget, and optional DeepSeek-style reasoning effort. Defaults avoid sending extra thinking/effort parameters.
- Verified with `node --check app.js`, direct local HTML Playwright DOM/state checks, the develop-web-game smoke client, and screenshots under `output/final-ui-request-qa`.
- Corrected the SiliconFlow model dropdown to show only exact raw model names (`Qwen/Qwen3.6-35B-A3B` and `Pro/moonshotai/Kimi-K2.6`) with no extra labels.
- Moved model-specific advanced request fields behind a collapsed `高级兼容参数` block, fixed the default max tokens fallback to 512, and kept normal setup focused on URL/model/key.
- Hardened the `模拟鉴定` button as an offline test path that does not require API, photo, or film and always creates a local test equipment item.
- Removed custom-mode model input duplication: preset mode shows the model dropdown, while custom mode hides it and shows a single writable `Model` field.
- Added visible feedback for `模拟鉴定`: the button briefly changes to `已生成`, the API result box reports the generated item, and the script URL now has a cache-busting version query so local browsers reload the latest JS.
- Replaced the misleading offline `模拟鉴定` and text-only `测试对话` controls with a single `测试图文模型` button. It generates a small local test image and sends a real OpenAI-compatible `image_url` request, while keeping only the result box in the UI.
- Removed user-facing advanced model parameters from the config panel. The game now uses internal defaults (`max_tokens=512`, image `detail=low`) and sends no thinking/budget/effort fields to avoid model-specific incompatibility.
- Split image compression into two levels: uploaded photos are resized to max edge 1024 / JPEG 0.78 for model analysis, and successful photo equipment is stored as a smaller max edge 420 / JPEG 0.72 image in the backpack.
- Added a small `window.__photoHeroTestHooks` object for automated image compression tests; verification showed a 1600x1000 test image becoming 1024x640 for analysis and 420x263 for inventory storage.
- Mapped the newly added `figure/039-01.png` to `魔龙` while keeping `魔王` on `figure/53.png`; verified floor 35/39/40 state, no random non-boss floor spawns `大法师`, and `魔龙` reward grants ordinary film plus direct speed +2 without adding a special film equipment item.
- Improved the desktop UI around opponent selection and API setup: added a dynamic enemy-selection hint explaining multi-select rewards, attack order, and escape behavior; removed the old custom API warning sentence; moved the API settings panel into an overlay contained within the left `照片勇者` column; unified button colors so most buttons share the neutral treatment and only important action buttons use the amber accent. Verified with `node --check app.js`, the develop-web-game client, targeted Playwright DOM checks, and screenshots under `output/ui-panel-hint-qa`.
- Hardened model鉴定 parsing after a real provider returned non-JSON text. `extractJson` now accepts strict JSON, fenced Markdown JSON, nested object wrappers, Chinese field names, relaxed single-quote/unquoted-key JSON, and OpenAI-compatible array content. If the model describes the image in plain Chinese but fails to output JSON, the game conservatively infers an item name/value and lets existing stat allocation clamp the equipment; if the model says it cannot view the image, the error now clearly says the model likely lacks image input support. Verified with `node --check app.js`, targeted Playwright parser cases, and a local HTML smoke run under `output/json-compat-qa`.
- Broadened response payload text extraction after a provider returned no `choices[0].message.content`. Both `测试图文模型` and real鉴定 now read common OpenAI-compatible and relay fields including `message.content`, `message.reasoning_content`, `choices[].text`, `output_text`, `output[].content[].text`, `answer`, `reply`, `response`, and `result`. If no text is found, the error includes a compact response-structure summary for debugging the relay. Verified with `node --check app.js`, targeted Playwright payload cases, and a local HTML smoke run under `output/payload-text-read-qa`.
- Treated natural-language rejected identifications as successful zero-stat equipment instead of hard failures. Replies such as "this is a cartoon frog, not a real-life equipable item, tooLarge=true/value=0" now create a `tooLarge` item with value 0 and all stats 0, preserving the photo in the backpack while respecting the game rules. Also replaced the generic "check vision/CORS" detail text with contextual hints for image-input, CORS, payload-shape, or format issues. Verified using the user's cartoon-frog failure text, `node --check app.js`, and a local HTML smoke run under `output/rejected-fallback-qa`.
- Changed rejected / non-equipable photo results into one-time healing rewards. A `tooLarge` item is still added to the backpack as a record and cannot be equipped, but鉴定 now restores 5-10 HP capped by max HP and logs the heal amount. Verified with a deterministic browser test where `卡通青蛙` healed 8 HP, stayed value 0 with all stats 0, and did not auto-equip; also ran `node --check app.js` and local HTML smoke under `output/rejected-heal-qa`.
- Revised rejected / non-equipable photo results into active one-use supplies instead of instant healing. They enter the backpack as `补给`, cannot be equipped, show a `使用` button in the detail panel, heal 5-10 HP when used outside battle, and are removed after use.
- Added equipment quality-of-life rules: fixed/system equipment with the same name can only have one copy equipped, consumables never equip, and the new `推荐装备` button fills up to 10 slots by stat value while skipping duplicate fixed names.
- Added a pure battle predictor for selected enemies and expanded the enemy hint to show selected reward, expected HP change, likely outcome, and upcoming Boss/reward-Boss preview. Boss floors still auto-select all enemies and cannot flee; reward-Boss floors remain optional.
- Reworked normal floor monster generation so duplicates are allowed (floor 1 can roll three 史莱姆) and floor difficulty is a weighted unlock curve: later floors add stronger monsters while earlier weak monsters can still appear.
- Moved the temporary attribute test controls into the same left-column overlay style as API settings, freeing the main game layout.
- Made model/API failures explicitly say that film was not consumed; successful rejected-photo identification still consumes film because it produced a usable supply.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; direct Playwright scripts for supply use/removal, duplicate fixed equipment, recommendation, selected-enemy estimate/reward text, victory summary, Boss floor auto-selection/no-flee, reward-Boss skip, repeated monster generation, debug overlay, and analyze failure/success film consumption. Also ran the develop-web-game smoke client and visually inspected `output/gameplay-loop-qa/shot-0.png`.
- Added API presets for 智谱 (`https://api.z.ai/api/paas/v4`, model `glm-5v-turbo`), 米醋中转 (`https://www.micuapi.ai/v1`, editable model), and TiMiCC (`https://timicc.com/v1`, editable model). 智谱 uses the locked dropdown model; relay presets lock URL but expose the model input because available model names depend on the user's relay account.
- Added image input shortcuts beside `选图/拍照`: `粘贴图片` uses `navigator.clipboard.read()` when available, and `截图` uses `getDisplayMedia()` to capture one screen frame. Both feed the same compression/preview/analyze path as file upload, and unsupported/denied cases now show visible status text near the buttons.
- Network probe without API keys: `https://www.micuapi.ai/v1/models` returned 401 with `Access-Control-Allow-Origin: *`, and OPTIONS for `/v1/models` plus `/v1/chat/completions` returned 204 with CORS headers, so browser direct calls look plausible once a valid key/model is supplied. `https://timicc.com/v1` returned 401/404 for GET but OPTIONS returned 403 with no CORS headers, so browser direct calls are likely blocked unless the service changes CORS or a proxy is used.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; DOM checks for all new presets and URL/model-field locking; Playwright checks for file input, paste/screenshot fallback notices, and the develop-web-game smoke client. Screenshot inspected at `output/input-presets-qa/shot-0.png`.
- Removed the TiMiCC preset after its CORS preflight failed, and added the Codex VIP relay preset (`https://api-vip.codex-for.me/v1`) with editable model. Network probe without API keys: `/v1/models` returned 401 `Missing API key` with `Access-Control-Allow-Origin: *`, and OPTIONS for `/v1/models` plus `/v1/chat/completions` returned 204 with CORS headers, so browser direct calls look plausible once a valid key/model is supplied.
- Removed the Codex VIP preset, added an OpenAI official link preset (`https://api.openai.com/v1`, editable model), updated provider links to the user's invite URLs for 米醋/智谱/硅基流动, expanded the 米醋 model dropdown to `gpt-5.5`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5.4`, `kimi-k2.5`, `kimi-k2.6`, `qwen3.5-plus`, `qwen3.6-plus`, and expanded the 硅基流动 dropdown with `Pro/moonshotai/Kimi-K2.5`, `Qwen/Qwen3.6-27B`, `Qwen/Qwen3.5-397B-A17B`, `Qwen/Qwen3.5-122B-A10B`.
- Hid the provider note text for 米醋 and OpenAI, added the 米醋文档 link (`https://docs.micuapi.ai/#/`), removed `gpt-5.4-nano` from 米醋 models, and changed API Key storage to be per preset so switching providers clears the visible key while returning to a provider restores its own saved key.
- Removed the OpenAI preset and removed the extra 硅基流动模型中心 link, leaving only 硅基流动邀请链接/API 文档, 智谱, 米醋中转, and 自定义 in the provider UI.
- Also removed the remaining player-facing "模型中心" wording from the model-disabled error hint; it now refers to the generic provider backend instead.
- Filtered `reasoning_content` / thinking fields out of model text extraction so `测试图文模型` and real photo identification only use final answer text; the vision test prompt now asks for one final sentence and caps `max_tokens` at 96 to reduce verbose responses.
- Broadened photo equipment rules: any real object not clearly larger than a person should be treated as game equipment, not only wearable gear. Added code-level correction so small/portable items like hammers, tools, chairs, books, cups, toys, and desktop objects are not converted into healing supplies just because a model says "无法装备"; oversized scenes/vehicles/buildings still become consumable supplies.
- Fixed Qwen/SiliconFlow reasoning-only responses after `content` came back empty and `reasoning_content` contained the useful output. Requests for SiliconFlow/Qwen now include thinking-disable hints, and formal photo identification can fall back to parsing `reasoning_content` while the test UI still avoids displaying reasoning text.
- Added automatic tower-equipment merging: 10 same-name same-level monster drops merge into one item one level higher, with stats doubled per level. Same-name different-level fixed equipment can now be equipped together, while same-name same-level duplicates are still mutually exclusive.
- Corrected monster image mappings from user screenshots: guard, wizard, swordsman, warrior, archmage, skeletonCaptain, and knightCaptain now point to the intended figure assets; mage remains on the available guard-looking placeholder until a true mage image is supplied. Removed vampire from the normal monster unlock pool so boss/reward-boss monsters only appear on their specific floors.
- Updated mage image mapping to `figure/11.png` after the user supplied the correct mage asset.
- Added photo-equipment-only special effects. Photo equipment can now roll one local special trait that consumes item value before normal stat allocation; tower drops and supplies never roll these traits.
- Implemented the ten requested special effects: kill-threshold attack/defense/shield/speed growth, temporary in-battle attack/defense growth, per-kill max HP growth, per-kill healing, speed-5 double strike, and attack-5 shield-crash damage. Permanent progress is stored on the equipment; temporary attack/defense resets when battle ends.
- Equipment detail now shows special-effect pills and a `特` badge in the backpack grid. Existing save version `5` stays compatible through normalization, and `app.js` cache query was bumped to `20260516-photo-specials`.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js styles.css index.html progress.md`; targeted Playwright tests for kill growth, kill heal/max HP, temporary attack/defense reset, double strike, and shield-crash damage; develop-web-game smoke screenshot plus visual inspection under `output/special-effects-qa`.
- Removed `gpt-5.4-mini` from the 米醋 model dropdown after live testing showed it could see images but produced unreliable equipment JSON; keep `gpt-5.5` as the practical 米醋 default for photo鉴定. Cache query bumped to `20260516-micu-models`.
- Replaced system equipment visuals with generated local WebP assets under `assets/items/`: 14 tower equipment icons plus the film icon. `makeSystemItemImage` now resolves local assets instead of Twemoji/SVG placeholders, upgraded tower items always keep the local system image, and old fixed tower equipment images refresh during save normalization. Monster and hero images were not changed.
- Added 7 switchable hero forms under `assets/heroes/`, with the requested bonuses: max HP +30, attack +2, lifesteal +1, regen +1, speed +1, defense +2, and shield +10. The avatar is larger, the current form is saved in the player save, and the player can switch forms from the hero panel at any time.
- Replaced the slipper system icon with a generated flip-flop sandal instead of a winter slipper. Removed the screenshot button and `getDisplayMedia` path; clipboard input now supports both the button-based Clipboard API and direct Ctrl+V paste events with image files/items.
- Moved tracked monster sprites from numeric `figure/*.png` names into `assets/monsters/` with semantic ASCII filenames (`slime.png`, `skeleton-captain.png`, etc.) and updated `getMonsterImageUrl` to use the new asset base path. The old ignored QQ source images and local output/test folders remain untracked.
- Removed the remaining tracked `figure/` assets after hero/monster images moved to `assets/`; the local `figure/` and `output/` directories were deleted before upload. Verified that runtime image elements load from `assets/heroes` and `assets/monsters` with no `figure/` references.
- Started the mobile-first player-header cleanup: the top hero row now only exposes `照片勇者` / `API配置` / `重开`, the form selector moved into a rectangular avatar card, visible hero stats are two compact text rows, HP/shield are rendered as separate bars, and the photo buttons are renamed to `拍照/上传` and `粘贴`.
- Verified the player-header cleanup with `node --check app.js`, `git diff --check`, the develop-web-game smoke client, desktop and 390px mobile Playwright screenshots, plus interaction checks for opening the form panel and API panel. The mobile check confirmed no horizontal overflow, no visible API status badge, no visible attribute-test switch, and API button state changing to ready after custom config fields are filled.
- Refined the hero summary again per mobile screenshot feedback: API config now uses a CSS-colored red cross / green check icon, the shield stat is back to current/max text, the separate shield bar is removed, and the HP label/bar sits inside the same bordered stat card to the right of the avatar.
- Removed fixed tower equipment and fixed boss rewards as active gameplay systems. Monsters no longer receive item prefixes, monster kills only grant `胶卷碎片`, and Boss / optional Boss floors no longer grant ordinary film or direct stat rewards. Save version is now `7`, so incompatible old saves are discarded instead of migrated. Deleted unused system item WebP assets except the film icon.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; direct Playwright checks for fresh floor enemy names/drops, per-enemy damage text, normal kill shard gain with empty inventory, Boss floor shard-only rewards, and mobile/desktop screenshots for the new enemy card layout. Local `output/`, `tests/`, and old `figure/` folders were removed before upload.
- Added one-shot monster-card flip-in animation when advancing to a new floor, with reduced-motion compatibility and no save-state persistence. Also fixed the default life form starting HP so a fresh game shows `80/80` immediately instead of `50/80`. Verified by direct Playwright checks for fresh load HP and floor-advance animation cleanup.
- Added card-flip combat pacing: unselected monsters flip to card backs when battle starts, defeated monsters flip down during battle, finished battles flip all cards face-up before advancing, and the next floor flips new enemy cards in. The settlement window disables battle/flee and shows a short transition hint. Verified with `node --check app.js`, `git diff --check`, and Playwright state/screenshot checks for start flip, defeat flip, reveal, and next-floor entry.
- Tightened the API/form overlay headers: removed the intro sentence under API setup, removed the current-form explanatory sentence, kept both `收起` buttons on the same line, stopped secondary overlays from stretching to the bottom when content is short, and fixed the ready API toggle so black expanded state uses white text and a white check. Verified with `node --check app.js`, `git diff --check`, direct mobile Playwright DOM checks, and screenshots.
- Rebuilt the mobile equipment panel around two large top cells plus a fixed 5x2 backpack grid. The left square now handles photo/upload and shows film as a one-decimal value, the right 2:1 cell handles photo identification and selected-item actions, and the old external photo/analyze buttons, paging controls, and page/equip counters were removed. Backpack cells now only select items; equip/unequip/use/discard happens in the top detail cell. Also made the API missing-state cross turn white on a black expanded button. Verified with `node --check app.js`, `git diff --check`, the develop-web-game smoke client, and mobile Playwright layout/action screenshots.
- Compact enemy cards for mobile: normal floors now show three enemy cards in one row down to 320px without horizontal overflow, with smaller portraits/stats and two-line shard/damage text so the estimate stays readable. Current battle reports now render newest round details at the top, above the current-battle summary, so the latest combat message remains visible without scrolling. Verified with `node --check app.js`, `git diff --check`, the develop-web-game smoke client, desktop 1366px check, and 390px/360px/320px mobile screenshots.
- Flattened the player/equipment/enemy UI pass: hero avatar/stat card heights were reduced, the equipment library no longer has a separate outer panel frame, the image cell only shows the film badge when no photo/equipment image is visible, equipment detail actions moved to the name row, and detail content now shows description/effects instead of rarity/equip/value metadata. Removed the enemy hint bar and changed monster stats to a player-style text layout without divided grid cells. Removed the old one-use healing supply behavior; photo `hp` is consistently displayed and applied as max HP, and max HP gains also increase current HP by the same amount. Verified with `node --check app.js`, `git diff --check`, develop-web-game smoke, mobile 390/320 screenshots, desktop 1366 screenshot, and a test max-HP equipment flow showing `90/90` after auto-equipping.
- Reworked the equipment system into fixed 5x2 slots. Empty slots are selectable, a fresh game starts on slot 1, new photos bind to the selected empty slot, and all filled slots are active by default. Removed equip/unequip UI and old equipped-id state; only a trash icon remains for discarding a selected item outside battle. Starting film is now `3.0`.
- Large equipment image clicks no longer open the file picker when a slot already has an item; they open a full-screen image viewer instead. Empty large image cells show only a camera icon plus the film badge, while the right detail cell no longer repeats film count or "image ready" notices.
- Updated enemy and battle presentation: enemy cards now show `胶卷 +0.1`, damage estimates render as `损失 -x` / `损失 +x` / `损失 ???` with green/red/neutral coloring, max-round or unwinnable predictions use `???`, and the battle report header/chip text was removed so only the report box remains.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; develop-web-game smoke; targeted Playwright checks for fixed-slot selection, third-slot item placement, no equip/unequip text, full-screen image viewer, fresh `胶卷 3.0`, no horizontal overflow at 390px and 320px, three enemy cards in a row, no battle-report title text, and enemy reward/damage text in the new format.
- Follow-up audit: rechecked the latest mobile requirements against static text search, Playwright interaction tests, and 320/390/768/1366 screenshots. Found and fixed one combat-display bug where enemy damage estimates turned into `损失 ???` during active auto-battle because estimates were disabled while `currentBattle` / `autoBattleTimer` existed; estimates now keep updating during battle. Verified existing-item image clicks open the full-screen viewer without file picker, empty-slot camera clicks open file picker, latest battle detail appears at the top of the report, over-lethal loss becomes red, unwinnable predictions remain `???`, no old visible UI copy remains in source, and mobile has no horizontal overflow.
- Improved photo-equipment identification quality. The vision prompt is now a structured RPG-design rubric covering main-object selection, small-object permissiveness, 5-20 quality scoring, weighted stat budgets, semantic stat mapping, and optional special-effect selection. Model parsing now preserves `objectType`, `tags`, `reason`, and `specialEffects`, and no longer mistakes a string `item` field for a nested object. Local balancing now uses name + object type + description + tags for too-large correction, stat fallback, and semantic special-effect fallback, reducing random or mismatched effects. Verified with `node --check app.js`, `git diff --check -- app.js`, targeted browser tests for stickers, real cars, scissors, cups, hammers, and `item` string/object parsing, plus the develop-web-game smoke client and screenshot inspection. Cleaned generated `output/` before commit.
- Reworked the latest tower/equipment layout into one combined frame: removed the separate enemy frame and standalone battle-report frame, moved battle logs into the shared equipment/info box, put the 5x2 equipment grid above the info box on mobile, and placed the grid beside the info box on desktop to reduce wasted space.
- Tightened equipment interactions: fresh games start with the first empty slot selected and show `胶卷 3.0`; empty slots use a camera icon and open photo/upload on a repeat click; filled slots show item info first and open the full-width image viewer on repeat click; dismantling replaces discard and returns `ceil(value * 0.5)` film with the refund shown on the button. Equipment slot quality now follows value tiers: common/rare/epic/legendary.
- Hardened tower rules: normal floors now scale monster stats with floor while retaining weak monsters in the pool, boss monsters are explicitly filtered out of non-boss floors, and only the proper boss/reward-boss monster on its designated floor drops `胶卷 +1.0`; normal monsters keep `胶卷 +0.1`.
- Updated enemy cards for compact mobile display: regular cards show only `攻 / 防 / 速` in that order, omit monster regen/lifesteal/shield from the stat grid, move `损失 -x/+x/???` directly under the stat row, remove the reward/loss frame, show `胶卷 0.1` without a plus sign, omit the `生命` label before HP, and hide the battle-report title text.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; targeted Playwright checks for no horizontal overflow at 390px/320px, 10 camera slots, item-quality classes, dismantle refund, empty/filled repeat-click behavior, frozen battle damage estimates, boss-only floor generation, boss full-film drops, enemy `攻/防/速` order, HP label removal, and hidden battle-report title. Also ran the develop-web-game smoke client and visually inspected desktop plus 390px/320px mobile screenshots. Cleaned generated `output/` before commit.
- Added two new hero forms from the latest supplied images and renamed the assets to semantic ASCII names: `form-greedy.png` for 财迷 and `form-angry.png` for 愤怒. 财迷 gives monster film drops +0.1; 愤怒 gives attack +5 and defense +5, but all monster film drops become 0.0.
- Updated empty equipment slot detail so `空装备格` and `胶卷 3.0` share the header row, and the instruction now says `再次点击空装备格拍照。` while the underlying file picker still supports choosing an existing photo.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md assets/heroes/form-greedy.png assets/heroes/form-angry.png`; targeted Playwright checks for 9 hero forms, mobile 3-column form grid, 财迷/愤怒 stat and drop changes, boss/normal drop display, empty-slot file picker repeat-click, loot settlement for +0.2 and no-loot cases, no mobile horizontal overflow, and a develop-web-game smoke run.
- Re-audited the previous tower/equipment/mobile requirements. Fixed two missed/regressed points: enemy cards now show film drop and estimated loss on the same compact row, and boss/reward-boss drop enemies always display/settle fixed `胶卷 1.0` regardless of 财迷/愤怒. Equipment quality and dismantle refunds now use the item `value` directly, so 5-9/10-14/15-19/20+ quality tiers and `ceil(value * 0.5)` refunds match the design rule even when stat allocation does not spend the full value.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js styles.css index.html progress.md`; targeted Playwright checks for normal-floor weak retention/no boss monsters, boss-only fixed film drops, 财迷/愤怒 normal-vs-boss drops, enemy stat order/HP label removal, same-row film+loss display, frozen damage estimates during battle, empty-slot wording, value-based quality classes, value-based dismantle refund, no mobile horizontal overflow, screenshot inspection, and develop-web-game smoke.
- Changed the photo acquisition flow: equipment slots only select slots now, while the detail panel owns the `拍照` button. After a photo is chosen, the detail panel shows a right-side preview plus an `鉴定` button; pending-photo state locks equipment slot switching, battle, and flee until identification finishes or the photo state is cleared. The successful `receiveItem` path still clears the pending photo and inserts the item into the pending slot.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md`; targeted Playwright checks that equipment-slot clicks no longer open the file picker, the detail `拍照` button does open it, chosen photos show a preview, slots/battle/flee lock while a photo is pending, pending-slot index does not change under forced clicks, mobile has no horizontal overflow, plus a develop-web-game smoke run.
- Removed ordinary-monster stat scaling by floor. Monster attack/defense/HP/speed now always come from the fixed user-provided table; later floors only change the random pool composition, not the stats of the same monster type. New games now get a fresh run seed, so normal-floor encounters are random per run but stable within one run. Floors 1-5 force at least one slime, and boss/reward-boss floors remain fixed. Verified with `node --check app.js`, `git diff --check -- app.js`, targeted Playwright checks for run seed randomness, floor 1-5 slime guarantee, boss-only floor composition, no boss monsters on normal floors, and high-floor fixed base stats, plus a develop-web-game smoke screenshot.
- Added Boss/reward-Boss card rewards and updated photo identification scaling. Archmage moved from floor 39 to floor 38, leaving floor 39 as a normal buffer floor. Defeating any Boss/reward-Boss now pauses floor advance for a 3-card reward choice using enemy-card styling: global film drop +0.1, current film +20% rounded up to 0.1 film, photo value minimum +2, or photo value maximum +2. Photo value range is now stored per run and injected into the model prompt; default remains 5-20. Stat weights changed to hp 1, attack 5, defense 6, speed 15, shield 3, lifesteal 8, regen 10. Greedy/angry now affect Boss drops too. Player attack/defense/speed can go below zero; speed <= 0 means no hero action, negative attack does not heal enemies, and negative defense increases damage taken. HP form switching now blocks leaving at 30 HP or less and removes the 30 current HP bonus when switching away. Verified with `node --check app.js`, `git diff --check -- app.js styles.css index.html progress.md`, targeted Playwright checks for floor 38/39, Boss reward selection, dynamic value clamping, Boss drops under greedy/angry/global bonuses, negative-stat combat, and HP-form switching, plus develop-web-game smoke and mobile reward-card screenshot inspection.
- Tuned two photo special-effect penalties: `速度-5，二连击` is now `速度-3，二连击`, and `攻击-5，附带当前护盾*0.5伤害` is now `攻击-3，附带当前护盾*0.5伤害`; the value cost stays 16. Verified with `node --check app.js`, `git diff --check`, and a Playwright hook check that base speed 2 becomes -1 and base attack 3 becomes 0 while the UI text shows the new `-3` labels.
- Fixed defeated monster cards replaying the flip-down animation during battle. Flip-down cards now settle into the stable face-down state after the CSS animation, with a timeout fallback for rerenders that replace the DOM node before `animationend`. Verified with `node --check app.js`, a targeted Playwright DOM test where one defeated monster stays face-down while another remains active, `git diff --check`, and a develop-web-game smoke screenshot.
- Adjusted dismantle refunds to the corrected camera-resource economy: refund is `ceil(score * 0.05 / 0.1) * 0.1`, capped at `0.8` film. Film display remains one decimal place. Verified 5/16/20-score items refund `0.3/0.8/0.8` through a targeted Playwright DOM check, plus `node --check app.js` and `git diff --check`.
- Changed the round-50 stall rule from per-kill reset to one cap for the whole selected multi-monster battle. Killing an enemy no longer resets the battle round counter, so low-damage builds cannot slowly grind several monsters forever. Damage-estimate simulation now uses the same whole-battle cap. Verified with a targeted Playwright test where the first enemy dies, round remains cumulative, and the remaining enemy flees at round 50, plus a develop-web-game smoke screenshot.
- Added a battle speed toggle before the battle button cycling through x1/x2/x4, with auto-battle interval set to 1000/500/250ms and saved in localStorage. Added restrained hit feedback: the defender scales briefly when damaged, so hero attacks pulse the enemy card and monster attacks pulse the hero avatar. Removed the old post-battle flip-up reveal; normal victories go straight to the next floor's incoming cards, and Boss/reward-Boss victories switch directly to the three reward cards. Verified speed cycling, hit classes/cleanup, normal floor advance without flip-up, direct Boss reward cards, `node --check app.js`, and a develop-web-game smoke screenshot.
- Updated photo special-effect stacking. Duplicate special effects now stack by equipped item instance: temporary attack/defense gain and caps scale with the number of matching items, shield-crash damage ratios add together, and the double-strike effect is now `speed -5, strike count x2` per item. Verified isolated Playwright scenarios for two double-strike items (speed -10 and 4 hits), two temporary attack items (+2 attack per damaging hit), two temporary defense items (+2 defense per hit taken), and two shield-crash items (attack -6 and shield damage ratio 1.0), plus recent round-cap, speed/hit animation, Boss reward, `node --check app.js`, `git diff --check`, and develop-web-game smoke screenshot.
- Fixed the post-battle extra flip on the last defeated monster. Defeated enemies only enter the flip-down state when more active enemies remain in the current battle; the final kill now goes straight to floor advancement, while earlier multi-enemy kills still flip down and settle face-down. Verified with targeted Playwright DOM checks for solo-final-kill and multi-enemy-mid-kill cases, `node --check app.js`, `git diff --check -- app.js`, and a develop-web-game smoke screenshot. Cleaned generated `output/` afterward.
- Reworked the core photo-identification contract so the model now outputs semantic appraisal fields (`subjectName`, `sizeClass`, `photoQuality`, `statAffinity`, `specialAffinity`) instead of final value/stats/effects. Local game rules now compute value from quality, allocate stats from affinity, choose optional special effects by budget and deterministic hash, and ignore direct model `stats/specialEffects` whenever the new semantic schema is present.
- Updated the camera-resource economy: photo identification only consumes film after the model response is parsed, the inventory image is generated, and `consumeFilm()` succeeds; API/model failures keep the pending photo and do not consume film. Dismantle refund is now `ceil(value * 0.04 / 0.1) * 0.1` film.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js`; targeted Playwright/browser checks for semantic value calculation, ignoring model-provided over-budget stats/effects, cup/scissors/landscape appraisal behavior, value-16 dismantle refund to `0.7`, and failed API identification preserving both film and pending photo. Also ran the develop-web-game smoke client and visually inspected the screenshot; cleaned generated `output/` afterward.
- Continued the identification refactor by persisting appraisal metadata on equipment (`photoQuality`, score, stat/special affinity, subject/type/size) and showing a compact explanation in the shared detail panel. Saved equipment now keeps its settled value/stats/special effects across reloads and across later photo-value range rewards, while new identifications still use the current run range.
- Tightened the prompt to require Chinese names/descriptions unless the photographed object itself contains necessary English text. The detail panel now separates the model's short description from the local appraisal summary (`画面x/15`, key quality reasons, stat tendency) with `white-space: pre-line`.
- Verification for this continuation: `node --check app.js`; `git diff --check -- app.js progress.md/styles.css`; targeted Playwright checks for semantic metadata display, reload stability, dynamic value-range preservation, failed API calls not consuming film, and value-16 dismantle returning `0.7` film. Mobile 390px screenshots confirmed no horizontal overflow and readable compact item details.
- Strengthened the appraisal prompt again around four explicit decisions: main-subject selection, only rejecting human-scale-or-larger scenes, quality scoring by clarity/subject area/background/real-photo/interest, and stat/special affinity rather than final numbers. Local safeguards now treat `isScene=true`, `isEquipable=false`, oversized `sizeClass`, and scene words such as road/landscape as zero-stat equipment even if the model also suggests speed or HP.
- Updated combat stall caps to depend on selected enemy count: 1 enemy = 100 rounds, 2 enemies = 150, 3 enemies = 200. Damage estimates and real battle setup use the same limit.
- Corrected dismantle refund to use actual effect value, not displayed quality value: `ceil((stats value + special value) * 0.04 / 0.1) * 0.1`. Verified `生命+5、防御+1` returns `胶卷 +0.5`.
- Reworked desktop layout so the hero stats, equipment grid, battle buttons, and enemy cards occupy the left half, while the shared info/detail/log panel occupies the right half and stays fixed height with internal scrolling. The outer panel now ends at the enemy-card bottom instead of growing with long battle logs.
- Real SiliconFlow test with the temporary key passed for data-URL image appraisal. A Wikimedia scissors photo produced `黑柄剪刀` with attack/lifesteal tendency and value 19; a Wikimedia road landscape produced zero value/stats after the local scene guard. Direct remote image URLs were rejected by the provider, so the game path must continue sending uploaded/captured photos as compressed data URLs.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js styles.css index.html progress.md`; targeted Playwright checks for dismantle refund, 100/150/200 round caps, scene rejection, desktop fixed-height log scrolling, and mobile 390px no horizontal overflow. Temporary `output/` and `%TEMP%\photo-hero-api-test` files were cleaned afterward.
- Added duplicate-photo and duplicate-object guards for photo equipment. The save now records successful image fingerprints and object identity keys for the current run; a repeated exact image is blocked before the API call, and a repeated recognized subject/object is blocked after model parsing but before film consumption. Dismantling an item does not clear these records, so the same object cannot be farmed repeatedly.
- Verification for duplicate guards: targeted Playwright checks showed the same data URL is detected, `Black Scissors` and `Silver Scissors` both normalize to `obj:scissors`, `Ceramic Cup` normalizes to `obj:cup`, and duplicate detection preserves film/inventory state until a genuinely new object is used.
- Ran a large real-image appraisal evaluation with the temporary SiliconFlow key through the actual browser/OpenAI-compatible image path. The test covered 210 attempted real-image samples in total: a mixed keyword/object set plus a stable 100-photo Picsum set. Effective model appraisals exceeded 100; failures were image-source problems such as 403/404/timeouts, not game parsing failures.
- Findings from the first pass: nonzero equipment values were too concentrated in epic/legendary, abstract/scene-like beautiful photos could be over-rewarded, "交通工具" accidentally matched the portable "工具" whitelist, some real buses/cars/large objects slipped through, and leftover stat budget often became unexplained HP.
- Appraisal rules were tightened after the evaluation: the prompt now rewards actively photographed, real, touchable, close-up small objects with clean backgrounds and explicitly downweights abstract light, scenes, real vehicles, buildings, rooms, furniture, people/animals as equipment subjects. Local guards now reject clearly oversized subjects before portable matching, value mapping uses a curved quality score, low-realism/abstract/scene words reduce score, leftover HP only appears for food/drink/plant/healing semantics, and special effects must pass local semantic checks instead of trusting arbitrary model candidates.
- Post-fix regression on 100 stable real photos produced 98 effective model appraisals: 48 rejected/zero-stat scene-or-large/abstract cases and 50 nonzero equipment cases. Compared with the earlier run, abstract bokeh and several vehicle/landscape cases are now zeroed, normal photos spread more into rare/epic instead of almost all high epic, and stat distribution is less HP-heavy. Remaining design question: whether natural small objects like grass, pine cones, water drops, and close animal parts should be allowed as playful equipment or further penalized as weak player interaction.
- Verification for this pass: `node --check app.js`, `git diff --check -- app.js index.html styles.css progress.md` with only CRLF warnings, targeted browser hooks for oversized buses, scissors, food/drink, abstract light, and noisy special-affinity outputs, mobile 390px no-horizontal-overflow check, and the develop-web-game smoke client. Temporary evaluation scripts/results/screenshots were kept only under ignored `output/` during work and should be cleaned before upload.
- Continued appraisal iteration per the later direction that high scores should not be limited to hand-held daily items. The prompt now explicitly allows clear and interesting small natural objects, toys, models, stickers, patterns, ornaments, and movable small objects, while still rejecting human-scale subjects, true vehicles/buildings/rooms/scenes, abstract light, and living-creature main subjects. Model output remains semantic-only; local code owns value, stats, and rare special-effect settlement.
- Increased value spread and reduced stat clustering. Value mapping now uses stricter quality caps so ordinary-but-clear subjects often settle in 10-18 instead of always 20; only clear, close, clean, interesting photos usually reach 20. Stat allocation now requires a semantic match for each stat, avoids attack/shield/HP as generic fallbacks, allows some unused budget, and makes random photo special effects very rare so most equipment shows visible, understandable stats.
- Real-image regression used the SiliconFlow temporary key on a mixed LoremFlickr/Picsum set. The largest successful run had 145/145 effective appraisals: value buckets were zero=48, 5-9=8, 10-14=27, 15-19=50, 20+=12 before the final local tightening; offline rebalance of the same semantic outputs after final rules gave zero=53, 5-9=8, 10-14=22, 15-19=50, 20+=12 with no flagged no-stat/effect-dominance issues. A final 20-image real API spot check covered scissors, shoe/sink, sticker, leaf/flower, keyboard/guitar, bottle, toy, bird/cat, road/sky/car/building/bokeh, and Picsum edge cases.
- Verification for this appraisal pass: `node --check app.js`; `git diff --check -- app.js index.html styles.css progress.md` with only CRLF warnings; targeted browser hook tests for scissors/laptop/cup/shoe/box/bokeh/bus/leaf/sticker; 145-image real API evaluation; offline rebalance replay; 20-image real API spot check; develop-web-game smoke screenshot inspection. Temporary `output/` artifacts should be cleaned before upload.
- Rebalanced equipment and special-effect economy again: HP is now worth 0.5 points per 1 HP, shield 2, lifesteal 6, and regen 8. HP equipment no longer heals current HP on equip, and kill-based HP gain now only raises max HP unless a separate heal effect is used. Special effects are now a unique passive: only the strongest equipped special effect is active, epic items roll specials at 33%, and legendary items always roll one special effect. Updated the special-effect table to the new thresholds/caps and added `回复翻倍` / `吸血翻倍`.
- Updated equipment quality and shield UX: quality tiers are now 普通 5-12, 稀有 13-16, 史诗 17-20, 传说 21+; dismantle refunds are fixed by tier at 0.2/0.4/0.6/0.8 film; newly equipped shield items immediately fill the current shield to the new max, while dismantling clamps it down to the remaining max.
- Reworked equipment slot and detail visuals: occupied slots are rectangular with a square image area plus a separate quality-colored name strip, selected slots keep their quality border/glow, and the detail panel now colors/glows by the selected item's quality.
- Verification for this pass: `node --check app.js`; `git diff --check -- app.js styles.css index.html` with only CRLF warnings; targeted Playwright checks for shield fill/clamp, quality thresholds, tier refunds, rectangular slot sizing, detail quality styling, and mobile no-horizontal-overflow; develop-web-game smoke screenshot inspected.
- Changed the Boss reward `最高价值 +2` to `最高价值 +3` so raising the photo-appraisal ceiling has a clearer chance to unlock 21+ legendary equipment. The reward card copy and the actual `photoValueMax` increment now both use +3.
- Balance analysis note: checked early tower progression around film collection, life pressure, and whether players are forced to skip or only fight slimes. Current rules make floor 10 the first hard wall: starting 3 film plus ordinary `0.1` film drops usually gives only about 3-4 photo items before floor 10, while floor 10 forces two skeletons plus skeleton captain. Common appraisal outcomes such as attack+2/defense+1, attack+2/lifesteal+1, shield+4, speed+1, hp+8/regen+1 do not reliably pass this gate. Recommendation to discuss before changing code: either lower the first boss gate, raise early film income, or add a guaranteed early power path; otherwise normal forms feel like slime-only farming and angry form becomes a no-reward dead-end.
- Early-boss tuning pass: floor 10 now contains only `skeletonCaptain` instead of `skeleton + skeleton + skeletonCaptain`. Damage estimates no longer show `损失 ???`; unresolved fights display concrete short reasons such as `破防不足`, `速度不足`, `回合不够`, `会倒下`, or `难以战胜`. Enemy-flee summaries now use the actual round cap instead of hard-coded `50回合`. Verified with `node --check app.js`, browser state checks for floor 10 composition and estimate labels, mobile screenshot inspection, and develop-web-game smoke; cleaned generated `output/` afterward.
- Added superlinear multi-enemy film rewards: in a selected multi-monster battle, the 2nd defeated enemy gets +0.1 film and the 3rd gets +0.2 film on top of normal/boss/form/global drops. Enemy cards preview the selected-order bonus, angry form still drops no film, and battle loot summaries now merge multi-shard resource entries correctly. Verified normal 3-enemy reward = 0.6 film, greedy 3-enemy reward = 0.9 film, angry 3-enemy reward = 0, and boss/reward-boss composition stays single except floor 30 (`warrior`, `warrior`, `knightCaptain`). Early balance check still suggests bat/mage/skeleton are severe without early equipment, so any monster nerf should be a targeted tuning pass rather than broad scaling.
- Story/card polish pass: bat speed changed 8 -> 6, mage attack changed 10 -> 8, and mage unlock moved from floor 4 to floor 5. Added short floor-entry narrative events for important normal floors and boss/reward-boss floors, plus a more flavorful clear message. Equipment descriptions now prefer the model text only when it matches the final settled stats/effects; if the model says defense/regen/etc. but the final item only has HP, the UI replaces it with a stat-consistent local description. Enemy card spacing was tightened for mobile: trait text can wrap to two lines, the name/trait gap is larger, and stats/drop/estimate/HP rows are denser. Verified with `node --check app.js`, cache-busted local page checks for floor narratives and boss composition, a regression item matching the air-purifier screenshot class, desktop and 390px mobile screenshots, and no horizontal overflow.
- Follow-up appraisal fix: the air-purifier case showed the model description was semantically right (`恢复/防御`) but legacy direct `stats` could still force HP. Added purifier/filter semantic matching, clarified in the prompt that `生命恢复/回血` belongs to `regen` rather than `hp`, and changed stat allocation so provided stats are only preserved when they match item semantics. Verified via browser hooks that `白色空气净化器` with a bad legacy `{hp:10}` output now settles as `回复+1、防御+1、生命上限0`, while preserving the original description.
- Failure-state UX fix: photo appraisal failures now automatically discard the pending photo, keep film unconsumed, and immediately unlock battle, flee, equipment-slot selection, and the photo button. The failure panel states that the failed photo was discarded and lets the player re-shoot. Equipment detail stat pills now use full labels such as `攻击`、`防御`、`回复` instead of one-character abbreviations. Verified with a forced API 500 browser test plus a smoke screenshot.
- Presentation cleanup: removed player-facing appraisal mechanics from equipment descriptions. Internal `photoQuality` and `statAffinity` are still stored and used for settlement, but the detail panel no longer shows lines like `画面15/15`、`主体清楚`、`倾向：攻击>吸血`. Updated the script cache key and verified with a browser hook that semantic appraisal metadata no longer leaks into item descriptions.
- Boss-selection and enemy-card spacing pass: Boss floors no longer auto-select enemies. Escape remains disabled, but battle is only enabled after the player manually selects every living Boss-floor enemy; floor 30 keeps player-chosen click order for the two warriors and knight captain. Enemy cards now use non-stretched grid rows with a middle-ground mobile min-height/line spacing so stats, film/loss, and HP bar stay grouped without becoming cramped. Verified floor 10 and floor 30 selection locks/order plus a 390px mobile screenshot.
- Added hero form leveling. Each form now tracks kills as `0/20`, upgrades to `Lv.2` after battle settlement, then stays at `20/???`; the form chooser cards place the avatar on the left, show `Lv.x` in the top-right, and show effect lines under the progress counter. Lv2 effects implemented: HP form gains `生命上限+40` plus post-victory max HP +3, attack ignores 25% enemy defense, defense immunes the first two damage hits, regen heals 6 after victory, speed pre-strikes every selected enemy before battle, shield converts shield loss into healing, greedy gains +0.1 film on flee, lifesteal heals 4 on kills, and angry is now `生气` with Lv1/Lv2 two-line no-film effects.
- Form leveling verification: `node --check app.js`; `git diff --check -- app.js styles.css index.html` with only CRLF warnings; targeted browser tests for all nine form upgrades, 19/20 multi-enemy post-battle-only upgrade timing, HP post-victory gain/no flee gain, attack ignore-defense damage, defense immunity, regen/lifesteal/shield Lv2 effects, greedy drop/flee film, mobile three-column form chooser with no horizontal overflow and left-aligned avatars; develop-web-game smoke screenshot inspected.
- Hero form and battle-effect audit: locked form/equipment changes during battle-start delay, active battle, and floor-advance/reward transitions so battle settlement cannot switch to the wrong form. Fixed Speed Lv2 pre-battle damage estimates so enemies killed by the free strike show concrete safe loss instead of unresolved. Corrected the photo special effect `每次击杀生命+8` so it heals current HP only, while `每次击杀生命上限+2` remains the permanent max-HP effect; special-effect eligibility no longer requires an extra 10 points of ordinary stat budget, so value-15/16 special effects can actually appear.
- Verification for the audit: `node --check app.js`; `git diff --check -- app.js` with only CRLF warnings; targeted Playwright tests for fresh HP form, post-battle-only leveling, locked form switching during battle and start delay, all Lv2 form effects, HP-form switch threshold, Speed Lv2 estimate/real pre-strike, kill-heal vs max-HP special effects, and stacking of double strike, shield-crash, deal-damage attack, and take-damage defense effects. Also ran the develop-web-game smoke client and inspected `output/form-audit-smoke/shot-0.png` before cleaning output.
- Boss reward cards now show an explicit `Boss 奖励 · 三选一` prompt and use the four supplied desktop icon sprites, copied into `assets/rewards/` with semantic filenames for film drop, current-film percent, minimum photo value, and maximum photo value. Verified all four reward types load the correct icon across Boss/reward-Boss floors, checked mobile and desktop reward screenshots, ran `node --check app.js`, `git diff --check -- app.js styles.css`, and a develop-web-game smoke pass.
- Added a top-row `游戏信息` panel button to the left of API settings and added a visible `切换` badge on the hero avatar so form switching is discoverable. The info panel now shows author, project URL, gameplay notes, form/Boss notes, and local API/privacy notes. Verified 320px/390px/mobile and desktop no horizontal overflow, visible full title, info-panel open/close, avatar form-panel open, `node --check app.js`, `git diff --check`, and a develop-web-game smoke screenshot.
- Balance/timeout pass: vampire defense changed from 6 to 0 while keeping high speed and lifesteal as its identity. Boss and reward-Boss floors now use a fixed 200-round cap, including floor 30's warrior/warrior/knight-captain fight. Surviving the cap clears the floor without loot or Boss reward choice. Verified floor 20 vampire defense, floor 20/25/30 round caps, normal one-enemy cap still 100, real 200-round Boss timeout advancing to floor 21 with `获得：无`, `node --check app.js`, `git diff --check`, and a develop-web-game smoke screenshot.
- Added anti-hang safeguards for photo identification. Vision test requests, photo identification requests, duplicate-object comparison requests, and image decoding now have explicit timeouts. While identifying, the detail-panel `鉴定` button becomes `取消鉴定`; canceling or timing out automatically discards the pending photo, keeps film unconsumed, clears the busy state, and unlocks equipment selection/battle. Verified cancel against a never-returning API route, automatic 45-second timeout recovery, normal successful identification still consuming exactly one film and creating equipment, `node --check app.js`, `git diff --check`, and a develop-web-game smoke screenshot.
- Hardened JSON and upload handling after a real response looked like valid JSON but still failed parsing. The parser now tries fenced JSON, JSON embedded in surrounding text, trailing explanation cleanup, and likely-truncated object completion. Upload compression now prefers `createImageBitmap` with EXIF orientation support and falls back to `Image`, rejects over-24MB images with a clear message, and wraps canvas export failures. Verified embedded/trailing/truncated JSON responses produce equipment such as `龙胆亮银枪`, full mocked identification succeeds without the failure panel, normal PNG upload compresses to JPEG, non-image/oversized uploads show clear errors, `node --check app.js`, `git diff --check`, and a develop-web-game smoke screenshot.
- Rebalanced photo rarity and special effects. The previous special-effect path effectively required model specialAffinity, max-value equipment, and a 16% roll, making special affixes almost invisible. Direct model specialAffinity now grants one matching special effect when semantic and value checks pass; inferred specials can still appear at lower probability on high-value strong-semantics items. Photo value caps now punish crowded scenes, small subjects, and non-clean backgrounds more strongly, so subjectArea<3 or backgroundClean<2 usually cannot reach epic. Verified strong scissors gets `dealDamageAttack`, messy many-object photos drop to common, medium cluttered photos stay below epic, a 54-case quality grid yields only 2 epic cases, `node --check app.js`, `git diff --check`, and a develop-web-game smoke screenshot.
- Suppressed fake/web/virtual equipment photos in the appraisal core. The prompt now tells the model not to reward web images, screenshots, game item cards, AI renders, illustrations, transparent-background assets, or pure virtual gear; real photographed cards/stickers/screens are treated as their physical carrier rather than as the fantasy weapon inside. Local rules now override the model when semantic text indicates a fake virtual image: clear web/search/game/AI/screenshot equipment images become zero-stat no-effect items, while physically photographed fantasy cards/stickers/screens are capped at common quality and cannot roll special effects. Real photographed toys/models/props remain valid and can still score normally. Verified with targeted Playwright hook cases for `龙胆亮银枪` web art, `狮纹金盾配剑` AI render, `恶魔之眼巨刃` game-card screenshot, a real printed weapon card, and a real toy spear model; also ran `node --check app.js`, `git diff --check -- app.js styles.css index.html progress.md`, and a develop-web-game smoke screenshot.
- Fixed hero form chooser card layout. Form cards now reserve the top row only for `Lv.x` on the left and kill progress on the right, while the form name and effect lines stay in the lower content area beside the avatar. Verified with a mobile Playwright DOM/layout check showing `form-card-meta`, `IMG`, and `form-copy` in the expected order, no horizontal overflow at 390px, screenshot inspection, `node --check app.js`, `git diff --check -- app.js styles.css`, and a develop-web-game smoke run.
- Fixed pending-photo action states. After taking/uploading a photo, the detail actions now show `鉴定` plus `放弃照片`, so the player can discard an unappraised photo without being forced to identify it. During appraisal, the identify button reliably changes to a readable red `取消鉴定` button; the previous white/blank-looking state was caused by referencing a non-existent `--red` CSS variable. Verified with Playwright interaction checks for pending, abandon, analyzing, and cancel states plus screenshot inspection.
- Reworked Boss reward selection to avoid accidental claims. Reward cards now only select/highlight a choice; the floor action row switches into a reward-confirm state where the original battle/flee action area becomes a single `选择` confirmation button. The button stays disabled until a card is selected, and only pressing it applies the reward and advances the floor. Verified with Playwright checks that card clicks preserve `bossReward`, selection persists in state, confirmation advances from floor 10 to 11, and the reward row returns to normal afterward; inspected the mobile reward screenshot.
- Removed the extra HP-equipment explanation line from item descriptions. HP equipment no longer heals current HP when first equipped; the detail panel also avoids the old extra-heal wording. Verified with a browser hook HP item detail check.
- Improved long equipment names and full-image quality styling. Equipment slot names can now wrap to two lines without increasing the slot size; the image area shrinks to make room instead of showing an ellipsis. Repeated-click full image preview now receives the selected item's quality and colors the large image border plus caption by rarity. Verified with a long-name epic item: slot height stayed fixed, the label used two lines, the image area shrank, and the image viewer showed epic purple border/text.
- Refined full-image quality styling so the enlarged equipment caption now includes the quality label itself, e.g. `传说 · 装备名`, while keeping the border and caption color tied to the item's quality. Verified with a legendary test item that the viewer uses `data-quality="legendary"`, orange border/text, and the expected caption.
- Unified empty-slot and common-quality equipment selection styling to a more visible light gray instead of near-white. Verified with browser checks that selected empty slots and selected common items share the same gray border/glow, plus a smoke screenshot pass.
- Fixed multi-enemy damage estimation around mid-battle death. The estimator still keeps a theoretical high-HP buffer for displaying full potential loss, but now tracks a separate real HP line and stops as soon as the real HP would reach zero, so later weak-enemy lifesteal/regen cannot falsely make a route look safe. Also marks exact lethal loss as danger. Verified targeted multi-enemy death, legal pre-heal survival, exact-lethal, real battle defeat, and a smoke screenshot pass.
- Reworked the game information panel into two tabs beside the collapse button: `拍照/API` for author, project link, physical-photo guidance, anti-web-image guidance, API/privacy; and `战斗/爬塔` for monster selection, speed as attacks per unit time, seven player stats, monster traits, Boss floors, rewards, and form leveling. Verified mobile and desktop tab switching, no horizontal overflow, and a smoke screenshot pass.
- Rewrote the information panel copy as an in-game `冒险手册` instead of developer-style rule notes. Tabs are now `影像装备` and `爬塔战斗`; photo guidance encourages real photographed objects through game-world wording, and battle guidance explains speed, losses, traits, Boss floors, film, forms, and rewards from the player's perspective. Verified mobile/desktop screenshots, no horizontal overflow, and removal of obvious developer phrasing from the manual.
- Adjusted the hero form chooser to the latest compact card layout: avatar in the upper-left, name/level/progress stacked on the right, and effect lines spanning the bottom. Single-monster Boss/reward-Boss cards now explicitly span the full enemy field width, so floor 10 `skeletonCaptain` matches other single Boss floors. Verified with mobile DOM checks, screenshot inspection, `node --check app.js`, and a develop-web-game smoke pass before upload.
- Updated two value-16 photo special effects: `doubleStrikeSpeedDown` is now `速度-3，连击翻倍`, and `shieldCrashAttackDown` is now `攻击-3，附带当前护盾伤害` with full current-shield bonus damage instead of the old 0.5 multiplier.
- Reworked Boss reward and film economy: normal and Boss enemies now each drop only `胶卷 0.1`, multi-enemy bonus film was removed, Boss reward choices always include `胶卷 +1.0`, and the current-film reward is now `当前胶卷 +50%`. The reward-phase speed button is hidden, and reward cards use a tighter mobile layout with fixed bottom status alignment and the existing scroll reward icon.
- Synced the in-game manual copy with the new film economy, removing the outdated note that second and third monsters give extra film.
- Restored the hero form chooser to the earlier large-centered-avatar card layout. The main hero card now only shows the current form name, while the chooser cards show `Lv.x + form name + kill progress` under the image and effect lines below.
- Changed equipment slot names into a bottom overlay on top of the item photo, so two-line names no longer shrink the image area or clip the second line.
- Release-facing polish: the in-game info panel now links the author name `落星峦` to Xiaohongshu, keeps the GitHub project link, and adds `研究生模拟器 v1.0` under other games. README was rewritten in a GitHub-friendly style with emoji links, play/author/project/other-game shortcuts, AI interactive exploration framing, tower combat notes, API requirements, and local preview instructions.
- Reworked normal-floor monster generation into weak/mid/strong slots. Early floors keep a weak entry for pacing, but from floor 5 onward every normal floor also rolls from mid/pressure pools so three early weak monsters no longer cluster and accelerate film farming. Verified multiple fresh-run samples across floors 1-40 with no post-floor-5 all-early floors and no normal-floor triple duplicates.
- Final Boss no longer opens the Boss reward three-choice flow. Clearing floor 40 now opens a career-summary share card, calls the configured OpenAI-compatible model for a social-share-style summary when available, falls back to a local summary without blocking clear, and keeps a reusable `生涯总结` button in the battle action row. Added a generated PNG share image download path and verified mobile/desktop clear states, no Boss reward, no horizontal overflow, and download filename generation.
- Boss reward choices are now dealt from a per-run no-replacement deck across the six reward floors (10, 20, 25, 30, 35, 38). Every choice set includes fixed `胶卷 +1.0`; the remaining 12 reward cards are limited to `当前胶卷 +50%` x5, `最高价值 +3` x3, `最低价值 +2` x3, and `胶卷掉落 +0.1` x1, with no duplicate pair in the same choice set. Verified floor 40 produces no reward choice even through the test hook.
- Release QA polish pass: removed the player-facing escape button, changed the disabled combat action to `选择怪物`, hid raw appraisal value-range copy from the equipment detail empty/pending states, simplified Boss reward card footers to avoid duplicate instructions, collapsed the equipment grid after final clear so the career summary becomes the main share view, and changed greedy Lv2 from a removed flee reward to `胶卷掉落 +0.2`. Added `scripts/review-qa.cjs`, `npm run qa:review`, and Playwright as a dev dependency for repeatable mobile/desktop regression checks. Verified `node --check app.js`, `node --check scripts/review-qa.cjs`, `git diff --check`, and `npm run qa:review` with no scenario failures.
- Added `docs/appraisal-regression.md` as the appraisal-module regression table. It captures the current scoring/quality/stat/special/duplicate rules plus release-test cases for real small objects, cluttered photos, cards/screens, web/AI images, oversized scenes, duplicate objects, timeout/cancel/non-JSON failures, and manual result logging. README now links the QA script and the appraisal regression table.
- Small reward-stage UI fix: the floor/action label now uses the Boss reward source floor, e.g. `第 10 / 40 层 · 奖励`, instead of showing the current normal floor during synthetic or restored reward states. Re-ran `npm run qa:review` and inspected mobile reward screenshots.
- Added lightweight global play statistics in the game info panel using the same shared-counter style as `kw66.github.io`: `increment_counter` and `get_counters` only, no table inserts and no heartbeat. It tracks total/today visits, visitors, and real battle starts; local preview does not increment counters.
- Rebalanced two temporary battle affixes: `造成伤害临时攻击+1` and `受到伤害临时防御+1` now cap at +4 per equipped item, so multiple copies stack as +4 * n. Also changed battle-special save normalization to clamp against the currently equipped dynamic cap. Verified with browser-level special-affix tests for 1x/2x attack and defense caps, then re-ran `npm run qa:review`.

- Desktop layout polish: the right-side equipment/info console now stretches with the desktop grid so its bottom aligns with the monster card row instead of using a fixed 584px height. Desktop-only film and photo/action controls in the detail header were enlarged to better match the wide panel. Verified with 1366/2048px Playwright measurements, screenshot inspection, node checks, git diff check, npm run qa:review, and the develop-web-game smoke client.

- Desktop manual panel polish: the info/manual overlay is now width-limited on desktop (min(880px, calc(100% - 16px))) so the adventure handbook no longer stretches across the full game panel with empty right-side space. Mobile keeps its full-width overlay. Verified with 2048/1366/390px Playwright screenshots, node checks, git diff check, npm run qa:review, and the develop-web-game smoke client.

- Appraisal API guidance: the empty equipment detail panel and pending-photo panel now detect missing API config and tell the player to use the top-right API configuration before identifying photos. Pending-photo identification is disabled until base URL/model/key are complete, and config input changes immediately refresh the detail panel. Added a review QA assertion for the missing-API prompt and verified no-API/with-key/pending-photo states plus node checks, git diff check, npm run qa:review, and the develop-web-game smoke client.

- Desktop photo input polish: the equipment/info panel now accepts dragged image files and focused paste images, while outside-page paste stays ignored. Added the cropped Xiaohongshu group QR to the author/stat info page as a QR-only square asset, tightened the info panel QR card, added API-missing guidance for appraisal, and expanded QA coverage for QR loading and desktop/mobile review scenarios. Verified with node syntax checks, git diff --check, npm run qa:review, targeted drag/paste/QR Playwright checks, and develop-web-game smoke screenshots.

- Info/stat release polish: author and project links now share the left side of the author block, the Xiaohongshu group QR sits on the right with the label "加入小红书游戏群", global stats now show heat plus adventure labels ("通关", "击杀", "鉴定", "爬塔层数"), and desktop secondary panels use compact content widths instead of stretching across the board. Verified with "npm run qa:review", syntax checks, diff whitespace check, develop-web-game smoke, and desktop/mobile screenshots under output/review-* and output/panel-layout-check/.

- Appraisal error hardening: loose model JSON recovery now extracts usable fields from mixed text such as a narrated JSON-like response, malformed JSON errors are shortened for mobile, old long error states are sanitized at render time, failed appraisal keeps film and unlocks retake/battle, and the error detail scrolls inside the fixed info box. Verified with "node --check app.js", "git diff --check", a targeted Playwright half-JSON/error-state reproduction, "npm run qa:review", and develop-web-game smoke screenshots.

- Appraisal consistency fix: stat allocation now uses object identity evidence (name, subject, type, size, visual identity) instead of model-written player description/reason text, so vague words like repair/energy cannot turn scissors into regen gear. Model stat affinity is sanitized against local semantics, low-value items are raised only to afford a valid preferred stat, sharp-tool recovery-only descriptions are rejected and rewritten, and `scripts/appraisal-regression.cjs` now covers scissors, purifier, cup, and fan consistency cases. Verified with syntax checks, appraisal regression, review QA, and develop-web-game smoke.

- Monster rebalance pass: updated early/mid monster stats to make slime, bat, orc, guard, and stone golem more distinct; softened skeleton, mage, wizard, warrior, skeleton captain, vampire, and octopus as requested; changed floor 30 from two warriors plus knight captain to two guards plus knight captain and updated the floor narrative. Verified with `node --check app.js`, `git diff --check`, appraisal regression, `npm run qa:review`, and develop-web-game smoke screenshot.

- Equipment special-economy pass: updated photo stat values to HP `0.5`, attack `5`, defense `6`, speed `12`, shield `2`, lifesteal `6`, regen `8`; HP equipment now only raises max HP and no longer heals current HP. Photo special effects now use the requested thresholds/caps, add `回复翻倍` and `吸血翻倍`, and obey a unique-passive rule where only the strongest equipped special effect is active. Specials now roll only on epic items at 33% and always on legendary items. Updated the appraisal prompt, regression document, and regression script. Verified with `node --check app.js`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and develop-web-game smoke screenshot `output/web-game-smoke-special-economy/shot-0.png`.

- Hero opening balance pass: bumped save version to 17, changed fresh hero base shield to 3, changed attack form to attack +3 / defense -1, changed defense form to defense +3 / attack -1, and made hero-form stats use signed normalization so the negative tradeoff is real rather than clamped away. Normal-floor weak-anchor monsters now progress by tower phase: early floors guarantee slime, then transition to bat, then to skeleton, while keeping the other slots random.

- Hero form presentation pass: changed form evolution threshold from 20 to 12 kills/experience, removed visible `Lv.` labels, and now presents evolved forms as `megaX形态` with `经验x/12`. Tightened the mobile form chooser cards by reducing avatar height/top whitespace while keeping three cards per row.

- Desktop form chooser layout pass: on wide desktop viewports the form chooser expands across the board and shows all 9 forms in one row; narrower desktop viewports keep the safer centered 5+4 two-row layout. Verified 1120/1280/1440px measurements with no horizontal overflow.

- Monster stat tuning pass: slime changed to 18 HP with regen 1, bat changed to 16 HP / attack 8 / speed 6 / lifesteal 1, and orc defense changed to 7.

- Boss trait pass: save version bumped to 18 and boss/reward-boss identities were expanded. Demon pressure now stacks attack -1 after each demon attack; dragon roar stacks speed -1 after each dragon attack and dragon speed is 3; octopus is 120 HP / 1 attack / 0 defense / 4 speed with giant attack based on the max-HP gap; archmage is 72 HP / 16 attack / 5 defense / 4 speed and gains attack when hit plus defense after attacking; knight captain is 40 HP / 15 attack / 3 defense / 4 speed and takes 50% less damage per living guard. Real battle and damage-estimate simulation both use these dynamic traits.

- Photo-save pass: stopped leftover local Playwright/http-server processes, changed slime HP to 20, added a post-appraisal Save button for equipped photo gear, and kept a higher-resolution `fullImage` on newly identified items so save/fullscreen preview use the appraisal image instead of the equipment thumbnail. Desktop prefers the browser Save File Picker, mobile prefers Web Share where available, and both fall back to normal browser download. Verified with `node --check app.js`, targeted mobile Playwright smoke, and `npm run qa:review`; confirmed no test server/headless browser processes remain.

- Appraisal realism pass: tightened the vision prompt and local fallback scoring for low-reality toy/mascot/model images. Real toys still work when the photo shows physical evidence, but black/white/transparent isolated toy/cartoon/product-style images are capped to ordinary low value, suppress specials, and get grounded descriptions instead of fantasy action copy. Added an appraisal regression case for the `多足寿司玩偶` failure mode and verified it now becomes a value-5 no-stat item with non-action description. Re-ran `node --check app.js`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, and `npm run qa:review`.

- Guard shield combat-state fix: confirmed `卫兵`护阵 was applied at battle start but could be reset by encounter normalization during render/load. Combat-state normalization now preserves in-battle monster shield/stat changes, monster HP display includes current shield as over-cap HP (`90/50` style), and hit animations trigger on attack attempts even when no HP/shield is lost. Also applied the requested mega attack/defense form changes (`mega攻击` -> 攻击 +4, `mega防御` -> 防御 +4). Verified with `node --check app.js`, `git diff --check`, targeted Playwright checks for floor 30 guard shields + reload persistence + no-damage hit effects, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and develop-web-game smoke screenshot `output/guard-shield-smoke/shot-0.png`.

- Appraisal virtual-image refinement: changed prompt and local validation so normal photos of real objects can pass even if sourced from a webpage/screenshot, while game item art, AI/animated/illustrated assets, transparent cutouts, polished commercial product renders, and fantasy equipment images remain suppressed. Added negation handling so text like "不是游戏装备图" no longer triggers the virtual-image penalty. Shortened appraisal failure copy into in-game wording instead of exposing rules/JSON/CORS details. Added regression cases for `雷蛇游戏鼠标` passing as a real peripheral and `游戏装备截图` staying invalid. Verified with `node --check app.js`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and develop-web-game smoke screenshot `output/appraisal-real-object-smoke/shot-0.png`.

- Appraisal wide-photo false-positive fix: broadened the prompt and local validation so wide real photos, room/table scenes, cluttered backgrounds, and screenshots of normal real-object photos can still identify a clear small physical subject instead of being rejected as web/virtual images. Added `hasClearRealSmallSubjectText`, protected small real subjects from oversized/digital penalties, narrowed speed semantics so generic electronics do not become speed gear, and added regression coverage for a wide indoor speaker photo plus a real-object screenshot while keeping game weapon screenshots invalid. Verified with `node --check app.js`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and develop-web-game smoke screenshot `output/web-game-appraisal-fix/shot-0.png`.

- Hero mega form economy pass: changed mega shield form to `护盾 +15` plus shield-loss healing, and changed mega greedy form to keep `胶卷掉落 +0.1` while converting carried film into attack/defense/speed in a repeating cycle: 1.0 film = attack +1, 2.0 = attack +1 defense +1, 3.0 = attack/defense/speed +1, 4.0 = attack +2 defense +1 speed +1. Added review QA coverage for shield and greedy film thresholds. Verified with `node --check app.js`, `node --check scripts/review-qa.cjs`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and screenshot `output/review-mobile-form-economy.png`.

- Hero form image alignment fix: measured hero PNG alpha bounds and confirmed the shield form asset is a wide `482x251` image, so the mobile form chooser's global top-aligned image rule made it look too high. Added a per-form key on form cards and applied a shield-only visual correction (`object-position: center`, wider width, no mobile upward translate). Verified with mobile/desktop form chooser screenshots under `output/form-shield-align/` plus `node --check app.js`, `git diff --check`, and `npm run qa:review`.

- Clear-state equipment access pass: final clear no longer collapses the equipment grid. After通关, clicking an equipment slot switches the info panel from career summary back to equipment detail, keeps the original `fullImage` available for saving, and disables only new photo/appraisal actions. Empty clear-state slots show a neutral `空` marker instead of a camera. Reset still clears inventory, pending photos, and the saved full-image data for the next run. Added review QA coverage for post-clear equipment visibility, selected equipment detail, save button visibility, and retained full image.

- Mobile save fallback pass: equipment/career image saving now treats mobile browsers as a separate path. It first tries Web Share when available; if share is unavailable, blocked, or canceled, it opens the original image in the full-screen viewer with a `长按图片保存` hint instead of silently relying on the fragile `download` attribute. Tapping the image itself no longer closes the viewer, so mobile users can long-press it. Added review QA coverage for the mobile fallback viewer and bumped the app script cache key.

- Monster distribution pacing pass: normal-floor encounters no longer let the newest high-tier monster dominate the third slot immediately after unlock. The pressure slot now samples from the top two available tiers, and tier 3/4 monsters use an unlock-ramp weight curve so they appear as low-probability warnings before becoming regular threats. Sampled floors 1/2/3/5/8/11/13/15/17/21/23/29/31/37; floor 1 stays all slime, tier 3 does not appear before floor 11, floor 11 tier-3 rate is about 7.5%, and floor 17 tier-4 rate is about 3.5%. Added review QA distribution checks.

- Boss film drop pass: single Boss/reward-Boss monsters now have base drop `胶卷 0.3`, while floor 30 keeps two `卫兵` plus `骑士队长` at `胶卷 0.1` each so the floor still totals 0.3. The card preview, battle reward settlement, and manual copy all use the same drop rule. Added review QA coverage for floors 10/20/25/30/35/38/40.

- Appraisal high-score balance pass: normal direct real photos now get a stronger local quality bonus when the subject is clear, real, and recognizably equipable, so ordinary phone photos with `clarity=3 / subjectArea=2 / backgroundClean=1 / realPhoto=3` can reach high-rare/epic scores instead of being capped too low. Screenshots or web photos of real objects remain valid but do not receive the direct-photo bonus, so they stay capped below epic in regression. Updated the prompt scoring calibration and appraisal regression expectations; verified direct real fan/mouse reach 18, wide cluttered speaker stays 12, real-object screenshot stays 15, and game weapon screenshot stays invalid.

- Manual subject selection MVP: pending photos now support `圈定主体` before appraisal. The player can drag a crop box on the pending preview, confirm it, and the appraisal call sends only that cropped region while keeping the original photo for viewing/saving. Duplicate checks now distinguish `sourcePhotoKey` from cropped `photoKey`, so different subjects from one photo can be appraised while the same source+crop is still blocked. Added regression coverage for crop resizing and duplicate behavior, plus mobile crop UI screenshots under `output/crop-ui-check/`. Verified `node --check app.js`, `node --check scripts/review-qa.cjs`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and mobile crop drag/confirm smoke.

- Group-combat special pass: added two unique passive photo specials, `横扫` (50% damage spread to adjacent selected enemies) and `无双` (battle-only attack/defense +3 after each kill). Real combat, damage-estimate simulation, prompt affinity, semantic fallback, equipment descriptions, and review QA now cover both effects, including the edge case where sweep still splashes after the main target is killed. Verified syntax checks, appraisal regression, full review QA, diff whitespace check, and a develop-web-game smoke screenshot.

- Full-screen subject selection pass: moved pending-photo subject selection into the full-screen image viewer so players can drag a crop box on a large image, with confirm/cancel controls and QA coverage. The same pass lets top toolbar panel buttons close their own panels when clicked again, updates mega form display names to megaX without the trailing form suffix, and bumps static asset cache keys. Verified with syntax checks, appraisal regression, full review QA, diff whitespace check, screenshot inspection, and the develop-web-game smoke client.

- Linked monster trait fix: fixed real battle startup to re-read selected enemies by id after the pre-battle render, so guard team shields, warrior warcry, and similar battle-start auras mutate current state enemies instead of stale object references. Added review QA coverage for guard shields/over-cap display, warrior warcry, wizard defense break, patrol shield break, golem sturdy, octopus giant, demon/dragon debuffs, archmage promotion, knight guard reduction, and shield-crash special interaction.

- Sweep targeting correction: `横扫` now chooses up to two living active targets by the monsters' visual card order in `state.enemies`, not by the player's selected attack order. The damage-estimate simulation stores the same visual index on cloned enemies, and review QA now includes an intentionally mismatched selected-order case to catch regressions.

- Unique passive correction: photo special effects are now unique by effect key, not globally unique. Each different special key can be active once at the same time, while duplicate keys keep only the strongest/tie-breaker item. Kill effects, passive stat penalties, multipliers, temporary attack/defense, shield-crash, double-strike, sweep, and visible effect pills now use the per-key active set. Review QA covers multi-passive interaction plus same-key dedupe.

- Action-trigger combat rule pass: unified combat around attack/defended actions. Hero lifesteal and the temporary attack special now trigger once per hero attack action even at 0 damage, including mega speed pre-battle attacks; sweep remains spread damage and does not create extra attack actions. Hero regen and the temporary defense special now trigger once per enemy attack action even when shield/defense/immunity absorb the hit, including mega defense immunity. Archmage promotion remains attack-action based, and sweep hits still count as being attacked. Damage-estimate simulation now re-reads battle stats after each action so temporary action bonuses affect later hits. Updated the battle manual copy and review QA coverage.

- Mega action sync follow-up: moved battle clock creation after mega speed pre-battle strikes so temporary attack/kill effects and defeated enemies are reflected before normal turns begin. Monster attack traits now resolve once per enemy attack action, including multi-hit attacks, in both real combat and damage-estimate simulation. Expanded review QA so mega speed pre-strike must trigger double-strike, attack-action growth, lifesteal, and valid post-strike clock setup.

- Mega HP sharing fix: `mega生命` now shares its +40 max HP with all other forms once the life form has evolved, while the active life form still avoids double-counting that bonus. Form switching now compares the real max HP before/after the switch instead of subtracting the raw form HP config, so switching from `mega生命` to another evolved form no longer incorrectly drops max HP or current HP. Review QA now covers defense-form max HP before and after a `mega生命` kill bonus.

- Appraisal background-tolerance pass: shifted prompt and local scoring hierarchy so real physical/player-shot evidence is weighted before clean background or visual fun. Mild desk/room/life background no longer over-penalizes clear real subjects, while polished product images, AI/game/art assets, and clean web-style images remain capped and cannot receive specials. Ordinary-capped product images now preserve object semantics for stat allocation instead of being treated as generic paper/card carriers. Verified with syntax checks, appraisal regression, full review QA, diff whitespace check, and develop-web-game smoke screenshot.
- Manual copy polish: rewrote the battle tab in the game info panel for first-time players. The new copy focuses on enemy selection, card reading, core stats, turn order, monster traits, boss floors, and film rewards, while removing confusing internal terms such as form-specific healing triggers. Verified with mobile screenshot review, syntax checks, appraisal regression, review QA, and develop-web-game smoke.

- Hero form HP switch fix: changing away from HP form now subtracts the lost max-HP amount from current HP instead of only clamping to the new cap, so players cannot heal by toggling HP form on/off. Form chooser lock checks now use real max-HP differences including shared mega HP and equipment. Added review QA coverage for 60/80 HP form -> 30/50 attack form -> 60/80 HP form and low-HP switch blocking.

- Mega HP form rule correction: user clarified that `mega生命`'s own `生命上限 +40` is form-only, and only the kill-growth max HP is shared because it is written to base HP. Removed the accidental shared +40 form bonus, made form-preview stats use the target form's film-cycle bonus, and updated review QA to cover normal HP switching, mega HP switching, low-HP blocking, and kill-growth persistence across forms. Verified with targeted Playwright probes, syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, `git diff --check`, and develop-web-game smoke screenshot `output/web-game/shot-0.png`.

- Linked-trait defense-break audit: checked `阻击` against temporary defended-action defense. The positive-defense path was already correct: temporary defense is added first, then the wizard aura halves the resulting defense, so it is not bypassed. Found and fixed a related edge case where negative defense was incorrectly clamped to 0 by `阻击`; negative defense now stays negative and still increases incoming damage. Added linked-trait QA coverage for one wizard, two wizards, temporary defense under `阻击`, and negative-defense damage. Verified with targeted Playwright probes, syntax checks, `npm run qa:review`, and `node scripts/appraisal-regression.cjs`.

- Octopus giant display fix: changed the octopus trait copy from `生命差` to `生命上限差` and added dynamic monster attack display so giant monsters show their effective attack on the card instead of the base `atk: 1`. `render_game_to_text` now exposes `displayAtk` for enemy cards. Added review QA coverage that octopus effective attack is `41` at hero max HP `80` and that the trait copy contains `生命上限差`. Verified with syntax checks, targeted floor-25 Playwright probe/screenshot (`output/octopus-card-check.png`), `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and `git diff --check`.

- Linked monster trait lifecycle pass: split monster effects into battle-start results and live auras. Guard `teamShield` and patrol `breakShield` now apply once at battle start and their results persist after those monsters die. Warrior `teamWarcry`, wizard `defenseBreakAura`, skeleton/skeleton-captain `noLifesteal`, and knight `noRegen` now only count while the source monster is alive; warrior speed aura also refreshes remaining enemy clocks after the warrior dies. Real combat, damage-estimate simulation, enemy-card effective stats, and review QA were updated together. Verified with `node --check app.js`, `node --check scripts/review-qa.cjs`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, `git diff --check`, and mobile screenshot `output/trait-lifecycle-smoke.png`.

- Knight captain summon pass: floor 30 now previews as one wide `knightCaptain` boss card with `胶卷 0.3`. Starting the battle expands it into visual order `guard / knightCaptain / guard`, while the hero target order is left guard, right guard, then knight captain so the badges read `1 / 3 / 2`. Summoned guards are marked as summon-only and drop `胶卷 0.0`; retreat restores the original single boss card from the battle snapshot. Damage estimates also simulate the summoned guards before battle. Verified with syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, mobile screenshots under `output/knight-summon-qa/`, a develop-web-game smoke screenshot under `output/knight-summon-smoke/`, and `git diff --check`.

- Appraisal value mapping pass: photo quality now first produces a base 5-15 score and linearly maps that into the current run's dynamic value range, so `最低价值 +2` / `最高价值 +3` rewards shift the final range without changing raw photo-quality semantics. Added light/shadow realism evidence to the prompt and local scoring: contact shadows, real reflections, consistent lighting, color temperature, and natural blur/noise help real photos, while studio/product/AI lighting does not bypass virtual-image caps. Added regression coverage for dynamic value mapping and updated appraisal cases for the new linear scale. Verified with syntax checks, `git diff --check`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, and the develop-web-game smoke client.

- Boss growth tuning pass: changed `魔龙`龙威 from reducing hero speed to increasing its own speed by 1 on each attack, and changed `魔王` to 100 HP / 18 attack / 8 defense / 4 speed with `晋升` instead of `压制`. Removed the now-unused attack/speed-down battle-special paths, synced damage-estimate simulation, bumped save/cache versions, and updated review QA for the new boss growth behavior. Verified with syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a direct browser boss probe, and screenshot inspection of `output/review-boss-film-drops.png`.

- Knight captain economy pass: removed `骑士队长`'s `护驾` damage reduction and kept only the summon trait. Summoned guards now have base `胶卷 0.0` but still receive additive film bonuses, so `mega财迷` turns the floor-30 battle drops into `0.1 / 0.4 / 0.1` while the preview captain remains `胶卷 0.3`. Updated review QA and bumped save/cache versions. Verified syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, the develop-web-game smoke client, and a targeted browser probe.
- Archmage summon pass: changed floor-38 `大法师` to 72 HP / 10 attack / 5 defense / 3 speed with `魔攻` plus attack-action summoning. Floor 38 still previews as one wide boss card; starting battle expands to `法师 / 大法师 / 法师`, with hero target order left mage, right mage, then archmage (`1 / 3 / 2`). Side mages are summon-only enemies with base `胶卷 0.0`; when a side mage is defeated and face-down, the archmage can spend its attack to revive that card without damaging the hero, restoring it to the fixed target order. Damage estimates and review QA were updated for the same behavior.
- Desktop layout polish: trimmed the desktop info panel by 18px so its bottom aligns with the visible enemy-card bottom instead of the enemy-field container, and gave the desktop enemy field a 3px outline budget with visible overflow so selected-card borders/glow are no longer clipped. Verified with desktop 1420x768 measurement/screenshot plus develop-web-game smoke screenshot.

- Octopus damage-estimate fix: damage estimation now preserves the hero's real max HP before adding the theoretical simulation buffer, so `章鱼`巨物 uses the displayed real max HP instead of the artificial buffer. Added review QA coverage for the screenshot case where hero max HP 110 and defense 11 should make floor-25 octopus display `损失 -0` rather than `会倒下`. Verified syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a targeted Playwright probe, and develop-web-game smoke screenshot `output/damage-estimate-smoke/shot-0.png`.

- Career summary polish: reworked clear-run summary into a tower-history style `塔史` card, tightened mobile layout, removed duplicated header stats, stripped Markdown/title wrappers from model output, and changed the AI prompt/local fallback to a third-person old-Mota legend tone. The share image now uses the same tower-history framing. Added review QA coverage for `**标题**` / `【通关】` cleanup and visually checked `output/career-summary-polish-mobile.png`. Verified syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and `git diff --check`.

- Sound effects pass: added the four desktop MP3s under `assets/audio/` with ASCII filenames, wired `鉴定成功`, `分解`, `进入下一层`, and battle-hit sounds through a shared browser-audio helper. Battle audio is triggered from the same hit-marking path that drives card/avatar scaling, with cooldown and autoplay-safe unlock handling. Verified syntax checks, review QA, appraisal regression, web-game smoke, and a targeted Playwright probe covering all four sound keys.

- Background music pass: copied the requested 魔塔原声带 MP3 files into `assets/music/` with ASCII filenames, added the top-row `音乐` panel between `游戏信息` and `鉴定台`, and exposed separate SFX/BGM toggles plus low default volumes (`45%` SFX, `22%` BGM). BGM now routes by opening/area, Boss battle, reward choice, ending, and defeat states without preloading audio before browser interaction. Renamed the former `API配置` UI to `鉴定台` across the game and QA expectations. Verified `node --check app.js`, `node --check scripts/review-qa.cjs`, `git diff --check`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and a targeted Playwright probe for the music panel plus BGM keys (`opening`, `area1`, `skeletonCaptain`, `reward`, `area4`, `defeat`).

- Sound effect alignment audit: found and fixed a real miss case where `battleHit` was skipped when the same hero/enemy was hit again after the 90ms sound cooldown but before the 260ms hit animation finished. Hit sounds are now tied to every hit action while the visual hit animation is restarted independently; sweep/fixed damage no longer calls the hit marker twice. Added `review-qa` coverage for appraisal success, dismantle, floor advance, repeated enemy hits, repeated hero hits, and sweep multi-card animation. Verified syntax checks, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, `git diff --check`, a targeted Playwright repro, and the develop-web-game smoke client; visually inspected `output/review-sound-effects.png`.

- Onboarding and ceremony pass: added first-run empty-slot guidance, appraisal bench missing-config feedback, first battle hint, post-kill film hint, stronger Boss floor copy/card badges/victory summaries, story-like equipment descriptions, and a 10-equipment image gallery in the career summary/share image. Added review QA scenarios for onboarding, Boss ceremony, career gallery, and item-story copy. Verified syntax checks and full review QA.

- Sound effect alignment follow-up: changed hit sounds to force one `battleHit` event for every card/avatar hit animation, so sweep or other simultaneous hits no longer animate several cards with only one sound. Updated review QA to expect three hit sounds for a three-card sweep. Re-verified syntax checks, `git diff --check`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a targeted Playwright sound probe, and key screenshots.

- Audio volume polish: added Web Audio gain boost for SFX and BGM so 100% is audibly stronger than the previous HTML-audio cap, while keeping low default settings. Replaced native range visuals with a CSS-filled slider and JS-updated `--slider-fill`, fixing the 100% track not reaching the end. Added QA coverage for boosted gain and 100% slider fill, and verified with `npm run qa:review`, appraisal regression, and `output/volume-panel-100.png`.

- BGM preload and header copy pass: BGM tracks now preload after the first user interaction in the order players encounter them (`opening`, `area1`, floor-10 Boss, `area2`, etc.) and reuse cached audio elements for later switching. The top music button is now the compact `♪` symbol, `鉴定台` UI wording was shortened to `鉴定`, and the config panel explains that photo appraisal requires a vision LLM API stored locally. Replaced the Xiaohongshu post link with `http://xhslink.com/o/17XFWimxM94`. Verified syntax checks, full review QA, appraisal regression, and `output/audio-preload-ui.png`.
- Hero form naming polish: evolved hero forms now display as `超级X` instead of `megaX` in player-facing labels, while internal test/helper names stay unchanged. Added review QA coverage so the main hero label and form chooser must show `超级财迷` and no visible form label may contain `mega`. Verified syntax checks, `git diff --check`, full review QA, appraisal regression, and screenshot inspection of `output/review-mobile-form-economy.png`.
- Audio slider endpoint fix: volume range inputs no longer inherit global input padding/border, so both 0% and 100% visually reach the exact ends of the track. Review QA now checks the 0% fill, 100% fill, and zero padding/border on the sound sliders. Verified syntax checks, `git diff --check`, full review QA, appraisal regression, and visual screenshots `output/volume-zero-check.png` / `output/volume-full-check.png`.
- Monster animation pass: extracted 4-frame 32x32 sprite strips from the Magic Tower character sheets into `assets/monster-animations/` for all 19 in-game monster types. Enemy cards now render a CSS `steps(4)` idle sprite with staggered delays while preserving the old static image as an invisible fallback/alt source; defeated or face-down cards pause the animation. Verified mobile fresh and floor-30 summon screenshots, syntax checks, `git diff --check`, full review QA, and appraisal regression.
- Hit effect pass: added two short Magic Tower animation strips under `assets/effects/`, using a slash effect for enemy cards and an impact flash for the hero avatar through the existing `is-hit` state. Review QA now checks both hit-effect resources plus the monster idle sprites. Verified syntax checks, `git diff --check`, full review QA, appraisal regression, and visual hit-effect screenshots.
- Dark UI polish pass: reduced the overuse of transparent/textured surfaces after the tile background change. Wall/floor tile texture now stays mostly as page/hero-area atmosphere, while readable surfaces such as the info panel, equipment slots/detail, monster cards, reward cards, form cards, battle log entries, music controls, and stats blocks use solid deep-gray panels with clearer borders and stronger text contrast. Buttons were normalized into dark-gray secondary controls, blue active panel controls, and amber primary action controls; cache keys were bumped to `20260525-dark-polish`. Verified `git diff --check`, `node --check app.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/appraisal-regression.cjs`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, visual screenshots under `output/dark-polish-ui/`, and a contrast spot-check for key dark panels.
- Floor-card texture pass: restored the floor tile background for the player form card, player stat card, and monster cards per user direction, while keeping equipment/detail/info surfaces solid dark gray. Monster cards now use a dedicated floor-card background, warmer border, inset bevel, inner pixel frame, textured portrait well, and dark information plaques for name/traits, stats, HP, and estimate rows so the tile texture adds material without hurting readability. Cache keys were bumped to `20260525-floor-cards`. Verified `git diff --check`, `node --check app.js`, `node --check scripts/review-qa.cjs`, full `npm run qa:review`, and visual screenshots under `output/floor-card-polish/`.
- Monster card cleanup pass: user rejected the previous monster card direction as poor, so the card layout was simplified. Monster cards keep the floor tile background, but the stacked dark plaques were removed in favor of one title plaque plus thin internal separators for stats, estimate, and HP. This keeps material texture without making the card look like a collage of small black panels. Cache keys were bumped to `20260525-card-cleanup`. Verified syntax and visual screenshots under `output/card-cleanup/`.
- Monster card refinement pass: moved monster cards back to a solid deep-gray main surface for readability, while keeping the floor tile only inside the monster portrait stage. The HP/estimate area stays clean dark gray, abstract in-card Boss badges were removed, and reward-card hover/layout was normalized so reused `.enemy-card` styles do not distort rewards. Player form and stat cards continue to use the floor texture per user direction. Cache keys were bumped to `20260525-card-refine`. Visual checks cover mobile fresh/selected/Boss/reward and desktop fresh screenshots under `output/card-final-corrections/`.
- Locked equipment slots now keep the same solid deep-gray panel surface instead of becoming translucent over the floor texture during reward/blocked states. Verified with mobile reward screenshots and a style probe under `output/monster-card-refine/`.
- Audio recovery framework pass: separated BGM stability from the Web Audio gain path, keeping BGM on native HTML audio while SFX still uses boosted Web Audio when the context is running and falls back to native volume when suspended. Added repeated gesture/focus/pageshow/visibility recovery, a no-input BGM watchdog for paused/error/stalled playback, and safer BGM handoff that starts the new track before stopping the old one. Added review QA coverage for BGM pause recovery, watchdog recovery, AudioContext recovery, BGM switching handoff, and SFX post-recovery errors. Cache keys were bumped to `20260526-audio-recovery`.
- BGM loop/refresh guard pass: same-track game actions such as battle start, escape, and state refresh no longer restart or refresh the current BGM while it is already playing. Native audio looping is disabled; BGM now restarts manually after the track ends with a 1s pause, with a loop-hold guard so pause recovery/watchdog/state refresh cannot cut the pause short; entering a floor/state with a different BGM key still switches immediately. BGM preloading now skips the active audio element so it does not interrupt an in-flight `play()` during unlock. Review QA now checks same-track no-refresh and delayed loop restart behavior. Cache keys were bumped to `20260526-bgm-loop-guard`. Verified syntax checks, `git diff --check`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a targeted BGM loop probe, and web-game smoke screenshot `output/web-game/shot-0.png`.
- Equipment name/refund polish: long equipment names now insert a balanced display-only line break in equipment slots, detail titles, and career gallery item cards, keeping the top line equal or slightly longer instead of leaving a one-character second line. Dismantle refunds by quality changed from `0.2/0.4/0.6/0.8` to `0.3/0.5/0.7/0.9`, cache keys were bumped to `20260526-name-refund`, and review QA covers both balanced title wrapping and refund tiers.
- Shield avatar alignment fix: the main hero avatar card still top-aligned every form image, while the shield PNG is much wider/flatter than the other form portraits. This made the shield form look shifted upward in the top-left player card even though the form chooser already had shield-specific positioning. The main avatar card now exposes `data-form-key`, shield uses centered object positioning there, and cache keys were bumped to `20260526-shield-avatar`. Verified the shield alpha center moved from about 25.5px to 42.5px from the card top, with enough clearance above the form label; added review QA coverage for the main shield avatar alignment.
- Defeated equipment viewing fix: after player defeat, existing equipment slots are now still selectable for detail viewing and repeat-click large-image preview, while empty slots, photo appraisal, dismantle, battle, and other mutation paths stay locked. Cache keys were bumped to `20260526-defeat-equipment`; review QA now covers defeated-state occupied-slot selection, empty-slot lock, disabled dismantle/photo actions, and repeat-click image viewer.
- Desktop equipment detail image pass: desktop detail panel now uses its extra width to show the selected equipment's saved/full image beside the text and stat pills, while mobile keeps the compact text-only detail body. The new preview is desktop-only, inherits item quality color, opens the existing large-image viewer on click, and does not affect pending-photo crop previews. Cache keys were bumped to `20260526-desktop-detail-image`; review QA covers desktop preview visibility/click behavior and mobile hidden state.
- Intro floor and BGM fade pass: fresh runs now start on floor 0 with opening BGM and three mandatory intro film cards; selecting all three enables `进入魔塔`, grants 3.0 film, and moves to floor 1. BGM routing now switches by floor entry (including Boss/reward-boss floors before battle starts), same-floor battle start no longer refreshes music, and track changes crossfade by quickly fading the old track down while fading the new one up. Cache keys were bumped to `20260526-intro-bgm-fade`; review QA covers intro flow, floor-entry Boss BGM, same-floor no-refresh, and crossfade handoff.
- Intro/BGM follow-up verification: adapted automated test hooks for the new floor-0 intro gate, then re-ran `node --check app.js`, `node --check scripts/review-qa.cjs`, `git diff --check`, `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and the develop-web-game smoke client. Visual checks covered `output/review-mobile-fresh.png`, `output/review-intro-flow.png`, `output/review-sound-effects.png`, and `output/web-game/shot-0.png`; no new TODOs from this pass.
- Battle stat delta display pass: split displayed player base stats from effective battle stats so in-battle attack/defense/regen/lifesteal changes render as colored deltas instead of overwriting the original numbers. Wizard defense break now locks its penalty to battle-start defense, so later temporary defense gains are additive and visible in the panel; skeleton no-lifesteal and knight no-regen also render as red deltas. Cache keys were bumped to `20260526-battle-stat-delta`. Verified syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a targeted Playwright stat panel probe/screenshot `output/battle-stat-delta-panel.png`, and the develop-web-game smoke screenshot `output/web-game/shot-0.png`.
- Defeat ending pass: defeat now creates a `careerSummary` with outcome `defeat`, opens a matching tower-history ending instead of only locking the run, and keeps already-owned equipment viewable after defeat. The ending card/share image were restyled to the current dark-gray tower UI with separate clear/defeat accents, and cache keys were bumped to `20260526-ending-outcomes`. Verified syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, the develop-web-game smoke client, and visual screenshots/share images under `output/ending-outcomes/`.
- Shield economy fix: restored shield to 4 value per point and changed equipment score/quality/refund/summary output to use actual stat + special-effect power instead of raw photo value. This prevents `shield +4` from showing as epic and prevents stale `value: 21` items with low shield stats from showing as legendary unless a real special effect brings the score up. Cache keys were bumped to `20260526-shield-economy`; appraisal/review QA now cover shield-only and shield+special quality cases.
- Global stats semantics pass: added an adventure `装备` stat that preserves the old successful-equipment counter (`photo_hero_appraisals_*`), and redefined `鉴定` as unique successful-appraisal browsers using a local appraisal id plus once-per-browser total and once-per-day daily flags. Review QA now stubs the Supabase stats RPCs, checks the 8-card stats layout, counter-key mapping, per-equipment increments, and same-browser appraisal de-duplication. While rerunning full QA, fixed a BGM handoff edge case where a newly requested track could be considered playing while still at fade scale 0 before its play promise resolved; the new track now begins fade-in immediately and the old track remains available for crossfade. Cache keys were bumped to `20260526-stats-audio`.
- Intro film copy pass: unified the three floor-0 entry cards to the same `胶卷` title and the same reward icon, rewrote their descriptions into straightforward photo-chance copy, and changed the intro detail/log/selected foot text from `胶片` to `胶卷`. Cache keys were bumped to `20260526-intro-film-copy`; review QA intro expectations were updated for the unified wording.
- Boss reward naming pass: renamed the Boss choice rewards so `+1.0 胶卷` is `补给胶卷`, `掉落 +0.1` is `胶卷磁石`, `当前 +50%` is `胶卷倍增`, `最低 +2` is `泛用胶卷`, and `最高 +3` is `高级胶卷`. Cache keys were bumped to `20260526-reward-film-names`.
- Reward copy verification: re-ran syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, the develop-web-game smoke client, screenshot inspection of `output/web-game/shot-0.png`, and a targeted Boss reward UI probe confirming all five renamed reward cards render in-page.
- HP/tableware balance pass: changed HP scoring to match its allocation budget so `生命上限 +20` scores as 20 instead of ordinary-tier 10, and tightened tableware semantics so spoons/forks/chopsticks/bowls/plates are treated as physical items unless they clearly contain food or drink. Regression coverage now asserts pure `hp +20` is epic and `不锈钢汤勺` does not become pure max HP. Cache keys were bumped to `20260526-hp-tableware-balance`.
- HP/tableware verification: re-ran syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, a direct Playwright balance probe for `生命护符 hp+20` and `不锈钢汤勺`, plus the develop-web-game smoke client with visual inspection of `output/web-game/shot-0.png`.
- First-photo callout pass: entering the tower from floor 0 now focuses the first equipment slot and keeps the info panel on that empty slot instead of switching to the log. The photo button gets a one-time amber breathing callout, and clicking it immediately marks the first-photo tutorial as started so the callout disappears even before a photo is selected. Cache keys were bumped to `20260526-photo-callout`; review QA now checks slot focus, callout visibility, and click dismissal.
- Experience API mode pass: added a default `体验` preset before custom/provider presets. It locks the UI to a SiliconFlow Qwen 3.5 35B A3B model, shows only a masked non-viewable key placeholder, and sends no Authorization header from the browser. Local preview still routes to `/api/experience/chat/completions`; production now defaults to the Cloudflare Worker base URL `https://photo-hero-experience.1092043672.workers.dev`. Added `workers/experience-proxy.js` and `wrangler.toml`; the Worker reads `PHOTO_HERO_EXPERIENCE_API_KEY` as a Cloudflare Secret, locks the upstream model, rejects text-only requests, caps request size/tokens/temperature, checks origin, and applies a simple per-visitor rate limit. The real key is intentionally not committed to source.
- Experience API visibility rollback: kept the hidden `experience` preset, local proxy, Worker proxy, and regression script in source, but removed the visible `体验` button and restored the default UI preset to SiliconFlow direct configuration. This lets us keep testing new API routes without exposing a public default before choosing the best provider/model.
- Windows preview server fix: changed `server.js` direct-run detection to compare resolved file paths instead of `import.meta.url` against a relative `process.argv[1]`, so `node server.js` stays running on Windows and browser QA can reach `127.0.0.1:3000`.
- Verification after hiding the experience preset: `node --check app.js`, `node --check server.js`, `node --check workers/experience-proxy.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/experience-proxy-regression.cjs`, `node scripts/experience-proxy-regression.cjs`, `node scripts/appraisal-regression.cjs`, `npm run qa:review`, `git diff --check`, and the develop-web-game smoke client all passed. Visually inspected `output/web-game/shot-0.png` and `output/review-api-config.png`; the config panel shows SiliconFlow/Zhipu/Micu/Custom only, with no visible `体验` button.
- Intro film copy and selection pass: rewrote the floor-0 three-film title/detail/card copy into tower-themed onboarding for equipment-slot photo, appraisal-table API activation, and monster-card battle selection. Intro film cards now use the same numbered selection badge behavior as monster cards, support click-to-cancel and order re-numbering, remove the selected foot row that changed card height, and force a stable three-column/equal-height intro grid. Also softened the info-panel `封门 Boss` wording into in-world gate-guard copy. Verified syntax checks, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, `git diff --check`, targeted selected-card metrics (`215x116`, badges `1,2,3`), and visual screenshots `output/review-mobile-fresh.png`, `output/intro-selected-check.png`, and `output/web-game/shot-0.png`.
- Xiaomi API preset pass: added a visible `小米` API preset between SiliconFlow and Zhipu with base URL `https://api.xiaomimimo.com/v1`, invite/docs links, and model dropdown values `mimo-v2.5` / `mimo-v2.5-pro`; shortened `米醋中转` to `米醋` while keeping the `micu` preset id for saved configs. Cache keys were bumped to `20260527-xiaomi-api`. Review QA now verifies the visible preset order/labels and Xiaomi URL/model/link wiring. Verified syntax checks, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, `git diff --check`, the develop-web-game smoke client, and visual screenshot `output/xiaomi-api-config.png`.
- Drawing hero mode pass: turned the `照片勇者` title into a mode button that toggles between `照片勇者` and `画图勇者`, saving the selected mode in config while keeping existing game progress compatible. Drawing mode renames the resource/action surface to `画布`/`画图`, swaps intro cards and empty-slot icons to drawing language, opens a large canvas modal with brush/eraser, color swatches, size buttons, clear, and `放入装备格`, then stores the canvas as a pending `画作` for the normal appraisal flow.
- Drawing appraisal framework pass: added drawing-specific system/user prompts and local balance handling so hand-drawn fantasy items are not penalized as fake photos or oversized real-world objects. Drawings now use a separate quality score path, duplicate checks compare drawing identity, pending/career/battle copy is mode-aware, and spray paint remains intentionally deferred until the core canvas loop is tested with real players. Verification: `node --check app.js`, `node --check scripts/review-qa.cjs`, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, develop-web-game smoke screenshot `output/web-game-drawing-mode/shot-0.png`, and targeted Playwright screenshots `output/drawing-mode-check/modal.png` / `output/drawing-mode-check/pending.png`.
- Mode-switch equivalence follow-up: clarified `胶卷` and `画布` are the same underlying resource and made mode switching fully live for the game surface. Switching is now allowed even during an in-flight appraisal; selected intro cards, empty-slot camera/brush icons, floor monster card drops, boss reward cards, and action labels all immediately swap wording between photo and drawing modes. Generated equipment keeps its original source/name and does not transform when the global mode changes. Review QA added `mode-switch-equivalence` to cover intro selection preservation, monster/reward live text, icon switching, generated equipment stability, and analyzing-time switching. Verified with syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and develop-web-game screenshot `output/web-game-mode-switch/shot-0.png`.
- Drawing/audio polish pass: moved the `照片勇者` / `画图勇者` switch into the top tool row before `游戏信息`, removed the standalone title-row treatment and arrow, and kept the button text to the current `xx勇者` mode only. Mode switching now renders without re-ensuring BGM, so it no longer restarts or switches music. BGM recovery ignores already-playing tracks during normal gesture/focus recovery, only restarts true paused/ended/error/stalled cases, and crossfades by fading the old track down while the new track fades in. SFX playback now uses a prewarmed pool that does not reload active sounds and can grow briefly under overlap, reducing dropped hit sounds. Drawing appraisal prompts and local balancing now treat drawings as equipment concepts, strip `手绘/涂鸦/画布/纸面` wording from displayed names/descriptions, broaden fantasy stat semantics, and use drawing-specific quality scoring. Verification: `node --check app.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and visually inspected `output/web-game-draw-audio-polish/shot-0.png`.
- Drawing appraisal quality pass: rewrote the drawing prompt into a two-step appraisal flow: first inspect visible line/color/shape/composition evidence from the doodle, then infer one equipment subject. Quality scoring now explicitly separates subject recognizability, subject size/completeness, background/noise, drawing completion, line/color control, and aesthetic/creative appeal; local normalization accepts Chinese/English aliases for those fields. Attribute allocation is now more visual-evidence driven: generic `幻想武器` object types are ignored when the drawing evidence does not support a weapon, and `魔杖/法杖/剑/刀/弓/箭` names are only kept if the identity description contains matching visual evidence such as a long handle, blade, bowstring, arrowhead, etc. Unsupported magic-weapon names and descriptions fall back to visible-subject names such as `爱心护符`, with attack affinity removed when it is not supported. Added appraisal regressions for rough vs polished drawing quality, unsupported wand fallback, and real wand preservation. Verification: `node --check app.js`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, and visually inspected `output/web-game-drawing-appraisal-quality/shot-0.png`.
- Drawing color swatch fix: selected canvas colors now keep their actual swatch background during normal, hover, active, and selected states instead of being overwritten by the global dark button hover/active background. Selection uses a light check mark plus color-matched outer ring, and cache keys were bumped to `20260528-drawing-color`. Verification: `git diff --check`, full `npm run qa:review`, develop-web-game smoke screenshot `output/web-game-drawing-color-final/shot-0.png`, and targeted modal screenshots `output/drawing-color-selected/red-active.png` / `output/drawing-color-selected/blue-active.png`.
- Appraisal retry retention pass: API/model/cors/JSON/validation failures and manual appraisal cancellation now keep the pending photo/drawing input instead of clearing it, so players can retry directly without retaking a photo or redrawing the canvas. Retrying clears the stale error while the new request runs, failed retry keeps the input again, and the pending-detail copy now says the photo/drawing is still retained. Cache keys were bumped to `20260528-appraisal-retry`. Review QA added `appraisal-retry-retains-input`, which stubs a failing vision API response, verifies the drawing remains pending with 3.0 canvas uses, and confirms clicking `鉴定` sends a second request. Verification: syntax checks, `git diff --check`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, visual screenshot `output/appraisal-retry-retains-input/failure-retains-drawing.png`, and develop-web-game smoke screenshot `output/web-game-appraisal-retry/shot-0.png`.
- Xiaomi vision API compatibility pass: investigated the reported Xiaomi screenshots and aligned the preset with Xiaomi's image-understanding docs. The Xiaomi dropdown now keeps only official vision models (`mimo-v2.5`, `mimo-v2-omni`) and explains that `mimo-v2.5-pro` is not a photo/drawing appraisal model. Xiaomi requests now use the documented `api-key` header, send image content before text, disable thinking with `thinking.type=disabled`, and translate `max_tokens` into `max_completion_tokens`; the test formatter also rejects reasoning/prompt echoes that do not actually mention the test image text (`照片勇者` / `VISION OK`). Cache keys were bumped to `20260528-xiaomi-vision`. Review QA now stubs the Xiaomi endpoint and verifies request headers/body plus result formatting. Verification: syntax checks, `git diff --check`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, visual screenshot `output/xiaomi-vision-config/xiaomi-mobile.png`, and develop-web-game smoke screenshot `output/web-game-xiaomi-vision/shot-0.png`.
- Wizard defense lock pass: 巫师阻击现在按战斗开场锁定减防来源和基准防御，击杀巫师后本场战斗不会返还防御；受击临时防御仍只累计到原本的 `+5`，在单巫师 `-5` 开局下最多把面板从 `-5` 抵消到 `0`，不会变成净 `+5`。Cache keys were bumped to `20260528-wizard-defense-lock`, and review QA now covers one-wizard temp defense cap plus one/all wizard death persistence. Verification: `node --check app.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, and develop-web-game smoke screenshot `output/web-game-wizard-defense/shot-0.png`.
- Career tower-history copy pass: 塔史结局面板不再显示 `AI 塔史` / `AI 残页` / `模型生成失败` / `重新生成` 等出戏文案，改为 `塔顶旧史`、`旧塔残页`、`抄录未成` 等塔内叙事；原来的重新生成按钮在塔史面板隐藏，只保留保存分享图。Cache keys were bumped to `20260528-career-copy`, and review QA now checks defeat/clear tower-history UI does not expose AI/regenerate wording. Verification: syntax checks, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, develop-web-game smoke screenshot `output/web-game-career-copy/shot-0.png`, and visual checks of `output/review-mobile-career-markdown.png` / `output/review-mobile-defeat-ending.png`.
- Adventure stats split pass: 全站统计的“冒险”组从旧 4 项扩展为 7 项，旧 `装备` 计数继续作为 `照片装备` 使用，并新增 `画图装备`、`超级形态`、`击杀Boss` 计数。Cache keys were bumped to `20260528-stats-adventure`; review QA now checks 11-card global stats layout, legacy photo-equipment counter compatibility, drawing-equipment counters, super-form counters, and boss-kill counters.
- Adventure stats verification: `node --check app.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/appraisal-regression.cjs`, `git diff --check`, full `npm run qa:review`, `node scripts/appraisal-regression.cjs`, and the develop-web-game smoke client passed. Visually inspected `output/web-game-stats-adventure/shot-0.png` and `output/review-mobile-info.png`; the stats panel shows 11 cards with 7 adventure stats and no generic `装备` card.
- Experience preset MVP pass: made the `体验` preset visible and default again so players can start without configuring a key. The browser still sends the existing optimized chat/completions prompt to the proxy for now; real API keys remain server-side only through the Worker/local proxy Secret path. Basic proxy protection was tightened: only data-URL image inputs are accepted, individual compressed images are capped at about 2.5MB, raw JSON body is capped at 4MB, timeout remains 45s, and a simple per-instance 3-request concurrency guard returns `公共鉴定台繁忙` instead of queueing. Server-side fixed prompt assembly remains deferred because the prompt is still changing.
- Experience preset verification: `node --check app.js`, `node --check server.js`, `node --check workers/experience-proxy.js`, `node --check scripts/review-qa.cjs`, `node --check scripts/experience-proxy-regression.cjs`, `git diff --check`, `node scripts/experience-proxy-regression.cjs`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, and the develop-web-game smoke client passed. Visually inspected `output/web-game-experience-entry/shot-0.png` and `output/experience-default-config.png`; the default config panel shows `体验` active, locked model/key, masked non-viewable key, and no stored browser API key.
- Experience QA harness follow-up: `api-config` review QA now actively clicks the default `体验` model test before switching providers, stubs the proxy response, and asserts the browser request uses the experience proxy without an Authorization header while still carrying an in-game data-URL image.
- Experience QA harness verification: re-ran syntax checks for `app.js`, `server.js`, `workers/experience-proxy.js`, `scripts/review-qa.cjs`, and `scripts/experience-proxy-regression.cjs`; `git diff --check`, `node scripts/experience-proxy-regression.cjs`, `node scripts/appraisal-regression.cjs`, full `npm run qa:review`, and the develop-web-game smoke client all passed. Visually inspected `output/web-game-experience-entry/shot-0.png`.
- Cloudflare Worker deployment pass: logged in with Wrangler, deployed `photo-hero-experience` to `https://photo-hero-experience.1092043672.workers.dev`, and set the `PHOTO_HERO_EXPERIENCE_API_KEY` Secret in Cloudflare. Live Worker checks confirmed Secret presence, GET/OPTIONS behavior, CORS headers, and a real SiliconFlow `Qwen/Qwen3.5-35B-A3B` image request returning `图文模型测试成功：图片里写着照片勇者 VISION OK` when front-end-equivalent thinking-disabled parameters are included. Note: local `127.0.0.1:3000` still routes experience mode through the local proxy and needs a local `PHOTO_HERO_EXPERIENCE_API_KEY` env var, while non-local deployments use the Worker URL.
- Experience config simplification pass: when `体验` is selected, the config panel now hides the API Base URL, API Key, Model selector, provider link row, and redundant save button, leaving only the preset note and image-model test action. Switching to Xiaomi/SiliconFlow/Zhipu/Micu/Custom restores the normal address/model/key controls. Cache keys were bumped to `20260528-experience-simple`, and review QA now asserts both hidden experience controls and restored provider controls.
- Monster bestiary pass: added a `图鉴` tab to the game info panel. The bestiary is generated from the actual monster/boss data, separates normal monsters from Boss entries, and clicking an entry shows its animated portrait, base HP/attack/defense/speed, trait explanations, and appearance rules. Boss entries include the fixed floor and distinguish gate/reward/final fights. Cache keys were bumped to `20260528-bestiary`, and review QA now checks the bestiary tab, normal/Boss counts, Demon detail, portrait rendering, and section separation.
- Monster bestiary pagination pass: replaced the long bestiary list with a compact `普通怪 / Boss` paged viewer. Each page renders only one monster detail card with portrait, stats, appearance rules, and trait details; the pager preserves separate normal/Boss positions and keeps mobile from scrolling through a long card grid. Cache keys were bumped to `20260528-bestiary-pages`. Verification: `node --check app.js`, `node --check scripts/review-qa.cjs`, `git diff --check`, `node scripts/appraisal-regression.cjs`, `node scripts/experience-proxy-regression.cjs`, full `npm run qa:review`, the develop-web-game smoke client, in-app browser DOM check, and desktop/mobile screenshots under `output/monster-bestiary-pages`.
